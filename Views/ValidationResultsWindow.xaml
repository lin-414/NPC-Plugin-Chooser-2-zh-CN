<Window x:Class="NPC_Plugin_Chooser_2.Views.ValidationResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
        xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:l="clr-namespace:NPC_Plugin_Chooser_2.Localization"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:VM_ValidationResultsWindow}"
        Title="{l:Loc validateOutputResults}"
        Height="640" Width="1120"
        MinHeight="400" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        Style="{StaticResource ThemedWindowStyle}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SummaryText}" FontWeight="SemiBold" Margin="0,0,0,8" TextWrapping="Wrap"/>

        <!-- Run-level notes (e.g. broad SkyPatcher filters not evaluated) -->
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="8" CornerRadius="3"
                Background="#22808080" BorderBrush="#55808080" BorderThickness="1"
                Visibility="{Binding HasNotes, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding NotesText}" TextWrapping="Wrap" FontStyle="Italic"/>
        </Border>

        <!-- Filter -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{l:Loc filter}" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="1"
                     Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     ToolTip="{l:Loc filterRowsByAny}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
                <CheckBox Content="{l:Loc error}" IsChecked="{Binding ShowErrors, Mode=TwoWay}"
                          VerticalAlignment="Center"
                          ToolTip="{l:Loc showFindingsWhereThe}"/>
                <CheckBox Content="{l:Loc warning}" IsChecked="{Binding ShowWarnings, Mode=TwoWay}"
                          VerticalAlignment="Center" Margin="12,0,0,0"
                          ToolTip="{l:Loc showLikelyProblemsWorth}"/>
                <CheckBox Content="{l:Loc info}" IsChecked="{Binding ShowInfo, Mode=TwoWay}"
                          VerticalAlignment="Center" Margin="12,0,0,0"
                          ToolTip="{l:Loc showInformationalRowsExpected}"/>
            </StackPanel>
        </Grid>

        <!-- Cell selection + ExcludeHeader: highlight any run of cells (drag, or Ctrl/Shift-click)
             and Ctrl+C pastes just those cells, tab-separated, with no column header line. The
             buttons below the grid are what emit a full headered table. -->
        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding FilteredIssues}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Extended"
                  SelectionUnit="CellOrRowHeader"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  ClipboardCopyMode="ExcludeHeader"
                  Background="{DynamicResource SecondaryBackground}"
                  Foreground="{DynamicResource PrimaryForeground}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  RowBackground="{DynamicResource SecondaryBackground}"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                  VerticalGridLinesBrush="{DynamicResource BorderBrush}">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{l:Loc copySelectedCells}"
                              Command="ApplicationCommands.Copy"
                              InputGestureText="{l:Loc ctrlC}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="6,4"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SeverityText}" Value="Error">
                            <Setter Property="Background" Value="#55C03030"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding SeverityText}" Value="Warning">
                            <Setter Property="Background" Value="#55C07A20"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="{l:Loc severity}" Binding="{Binding SeverityText}" Width="80"/>
                <DataGridTextColumn Header="{l:Loc check}" Binding="{Binding CheckText}" Width="90"/>
                <DataGridTextColumn Header="{l:Loc nPC}" Binding="{Binding NpcDisplayName}" Width="160"/>
                <DataGridTextColumn Header="{l:Loc formKey}" Binding="{Binding NpcFormKey}" Width="150"/>
                <DataGridTextColumn Header="{l:Loc formID}" Binding="{Binding NpcFormId}" Width="80"/>
                <DataGridTextColumn Header="{l:Loc selectedMod}" Binding="{Binding SelectedMod}" Width="150"/>
                <DataGridTextColumn Header="{l:Loc issue}" Binding="{Binding Issue}" Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="{l:Loc winningSource}" Binding="{Binding WinningSource}" Width="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="{l:Loc details}" Binding="{Binding Details}" Width="1*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{l:Loc copyToClipboardTSV}" Command="{Binding CopyTsvCommand}" Height="30" Padding="12,0"
                    ToolTip="{l:Loc copyTheVisibleRows}"/>
            <Button Content="{l:Loc saveCSV}" Command="{Binding SaveCsvCommand}" Height="30" Padding="12,0" Margin="10,0,0,0"/>
            <Button Content="{l:Loc close}" Click="CloseButton_Click" Height="30" Width="100" Margin="10,0,0,0" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

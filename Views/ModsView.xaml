<reactiveui:ReactiveUserControl x:Class="NPC_Plugin_Chooser_2.Views.ModsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NPC_Plugin_Chooser_2.Views"
             xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
             xmlns:gong="clr-namespace:GongSolutions.Wpf.DragDrop;assembly=GongSolutions.Wpf.DragDrop"
             xmlns:reactiveui="http://reactiveui.net"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             x:TypeArguments="vm:VM_Mods"
             d:DataContext="{d:DesignInstance Type=vm:VM_Mods}"
             d:DesignHeight="600" d:DesignWidth="1000"
             Loaded="ModsView_Loaded">

    <UserControl.Resources>
        <local:OrbitalRadiusConverter x:Key="OrbitalRadiusConverter"/>
        <local:BooleanNegationConverter x:Key="BooleanNegationConverter"/>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:IsNotNullConverter x:Key="IsNotNullConverter"/>
        <local:ModStatusToolTipMultiConverter x:Key="ModStatusToolTipMultiConverter"/>
        <local:PathExistenceToBrushConverter x:Key="PathExistenceToBrushConverter"/>
        <local:ModSettingLinkButtonTooltipConverter x:Key="ModSettingLinkButtonTooltipConverter"/>
        <local:ModKeyToStringConverter x:Key="ModKeyToStringConverter"/>
        <local:AreModKeysEqualMultiConverter x:Key="AreModKeysEqualMultiConverter"/>
        <local:PercentageSizingConverter x:Key="PercentageSizingConverter"/>
        <local:PatchingModeToVisibilityConverter x:Key="PatchingModeConverter" />
        <local:ResourceModKeyTooltipConverter x:Key="ResourceModKeyTooltipConverter"/>
        <local:IsGreaterThanOneConverter x:Key="IsGreaterThanOneConverter"/>
        <local:PathToFolderNameConverter x:Key="PathToFolderNameConverter"/>
        <local:FolderLockStateConverter x:Key="FolderLockStateConverter"/>
        <local:NonPrimaryFolderVisibilityConverter x:Key="NonPrimaryFolderVisibilityConverter"/>

        <!-- Padlock drawn as vector geometry rather than a bitmap so it stays crisp at the ~11px it is
             rendered at, and so it can take its colour from the active theme. The body is shared; only
             the shackle swaps between closed and open, which keeps the two states visually aligned.
             Coordinates live in a fixed 14x20 canvas so the icon never resizes as the state changes. -->
        <Geometry x:Key="LockBodyGeometry">M3,8.5 L11,8.5 A2,2 0 0 1 13,10.5 L13,16 A2,2 0 0 1 11,18 L3,18 A2,2 0 0 1 1,16 L1,10.5 A2,2 0 0 1 3,8.5 Z M5.6,13.2 A1.4,1.4 0 0 1 8.4,13.2 A1.4,1.4 0 0 1 5.6,13.2 Z</Geometry>
        <Geometry x:Key="LockShackleClosedGeometry">M4,8.5 L4,5.5 A3,3 0 0 1 10,5.5 L10,8.5</Geometry>
        <Geometry x:Key="LockShackleOpenGeometry">M4,8.5 L4,5.5 A3,3 0 0 1 10,5.5</Geometry>

        <!-- Per-folder lock toggle shown inline between Browse and X in the Corresponding Mod Folder
             Paths list. Unlocked reads as a dimmed open padlock; locked as a solid amber closed one. -->
        <Style x:Key="FolderLockButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="3,0"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
            <Setter Property="Opacity" Value="0.35"/>
            <Setter Property="Tag" Value="{StaticResource LockShackleOpenGeometry}"/>
            <Setter Property="ToolTip" Value="Not locked. A Refresh rebuilds this list from the plugin master chain, so this folder is dropped unless something points at it. Click to lock it in place."/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <Viewbox Width="11" Height="15" Stretch="Uniform">
                                <Canvas Width="14" Height="20">
                                    <Path Data="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}"
                                          Stroke="{TemplateBinding Foreground}"
                                          StrokeThickness="1.7"
                                          StrokeStartLineCap="Round"
                                          StrokeEndLineCap="Round"/>
                                    <Path Data="{StaticResource LockBodyGeometry}"
                                          Fill="{TemplateBinding Foreground}"/>
                                </Canvas>
                            </Viewbox>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Value="True">
                    <DataTrigger.Binding>
                        <!-- The item is a bare string, so the lock state has to be read off the owning
                             VM_ModSetting. LockedFolderRevision is included solely so the binding
                             re-evaluates when a lock is toggled. -->
                        <MultiBinding Converter="{StaticResource FolderLockStateConverter}">
                            <Binding Path="."/>
                            <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                            <Binding Path="DataContext.LockedFolderRevision" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                        </MultiBinding>
                    </DataTrigger.Binding>
                    <Setter Property="Tag" Value="{StaticResource LockShackleClosedGeometry}"/>
                    <Setter Property="Foreground" Value="#C9A227"/>
                    <Setter Property="Opacity" Value="1"/>
                    <Setter Property="ToolTip" Value="Locked. This folder is kept, at this position, every time the mod is refreshed — use this for &quot;silent&quot; dependency mods that supply meshes or textures but no plugin. Click to unlock."/>
                </DataTrigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LinkButtonStyle" TargetType="Button">
             <Setter Property="Cursor" Value="Hand"/>
             <Setter Property="ToolTip" Value="Show NPCs for this mod"/>
             <Setter Property="BorderThickness" Value="0"/> 
             <Setter Property="Padding" Value="0"/> 
             <Setter Property="Foreground" Value="DodgerBlue"/>
             <Setter Property="Background" Value="Transparent"/>
             <Setter Property="HorizontalAlignment" Value="Left"/>
             <Setter Property="VerticalAlignment" Value="Center"/>
             <Setter Property="Template">
                 <Setter.Value>
                     <ControlTemplate TargetType="Button">
                         <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                              <TextBlock x:Name="PART_Text"
                                        TextDecorations="Underline"
                                        Foreground="{TemplateBinding Foreground}"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                                     <ContentPresenter ContentSource="Content"/>
                              </TextBlock>
                         </Border>
                         <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                 <Setter Property="Foreground" Value="Blue"/>
                             </Trigger>
                             <Trigger Property="IsPressed" Value="True">
                                 <Setter Property="Foreground" Value="DarkBlue"/>
                             </Trigger>
                         </ControlTemplate.Triggers>
                     </ControlTemplate>
                 </Setter.Value>
             </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/> <!-- Main Content -->
            <RowDefinition Height="Auto"/> <!-- Zoom Control Row -->
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0" x:Name="MainContentGridForSplitter">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" x:Name="LeftColumnForModList"/> 
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="300" x:Name="RightPanelColumn"/> 
            </Grid.ColumnDefinitions>

            <!-- Left Panel Content -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> 
                    <RowDefinition Height="*"/>    
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="0,0,0,1" Margin="0,0,0,5" Padding="5">
                    <WrapPanel Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal" Margin="0,2,15,2">
                             <TextBlock Text="Filter Name:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                             <TextBox Text="{Binding NameFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="120" VerticalAlignment="Center"
                                      ToolTip="Filter the mod list by display name&#x0a;Ctrl+Shift+C clears all search filters"/>
                        </StackPanel>
                         <StackPanel Orientation="Horizontal" Margin="0,2,15,2">
                            <TextBlock Text="Filter Plugin:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding PluginFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="120" VerticalAlignment="Center"
                                     ToolTip="Filter the mod list by the plugins each mod owns&#x0a;Ctrl+Shift+C clears all search filters"/>
                        </StackPanel>
                         <StackPanel Orientation="Horizontal" Margin="0,2,15,2">
                            <TextBlock Text="Filter NPC:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox ItemsSource="{Binding AvailableNpcSearchTypes}"
                                      SelectedItem="{Binding SelectedNpcSearchType, Mode=TwoWay}"
                                      MinWidth="80" VerticalAlignment="Center" Margin="0,0,5,0"
                                      IsEnabled="{Binding IsLoadingNpcData, Converter={StaticResource BooleanNegationConverter}}"
                                      ToolTip="Select NPC field to filter by (active after initial load)"/>
                            <TextBox Text="{Binding NpcSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     MinWidth="120" VerticalAlignment="Center"
                                     IsEnabled="{Binding IsLoadingNpcData, Converter={StaticResource BooleanNegationConverter}}"
                                     ToolTip="Enter NPC search term (active after initial load)&#x0a;Ctrl+Shift+C clears all search filters"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,2,15,2" VerticalAlignment="Center">
                            <CheckBox IsChecked="{Binding ShowMugshotOnlyMods, Mode=TwoWay}" 
                                      Content="Show Mugshot-Only Mods"
                                      ToolTip="If unchecked, mods that contain only images (no plugins or data folders) will be hidden from the list."
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock Text="Loading NPC data..." FontStyle="Italic"
                                   Margin="15,0,0,0" VerticalAlignment="Center"
                                   Visibility="{Binding IsLoadingNpcData, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </WrapPanel>
                </Border>
                
                <!-- ============================ VIRTUALIZATION FIX START ============================ -->
                <!-- Replaced ScrollViewer + ItemsControl with a single ListBox.
                     The ListBox has a correctly configured built-in ScrollViewer that enables virtualization.
                     The ItemContainerStyle removes the default selection highlight, making it look like an ItemsControl. -->
                <ListBox Grid.Row="1" ItemsSource="{Binding ModSettingsList}" x:Name="ModSettingsItemsControl"
                         BorderThickness="0"
                         Background="Transparent"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">

                    <ListBox.Resources>
                        <Style TargetType="ScrollBar" BasedOn="{StaticResource {x:Type ScrollBar}}">
                            <Setter Property="ToolTip"
                                    Value="{Binding DataContext.TotalModsLoadedText, RelativeSource={RelativeSource AncestorType=ListBox}}"/>
                        </Style>
                    </ListBox.Resources>

                    <!-- This style makes the ListBoxItems behave like the old ItemsControl items
                         (no selection highlight, no border, etc.) -->
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Focusable" Value="False"/> <!-- Prevents dotted focus rectangle -->
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                Padding="{TemplateBinding Padding}"
                                                SnapsToDevicePixels="true">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" 
                                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>

                    <!-- The ItemsPanel is the same, but it will now virtualize correctly inside the ListBox -->
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <!-- The ItemTemplate is the same, but RelativeSource bindings are updated -->
                    <ListBox.ItemTemplate>
                         <DataTemplate DataType="{x:Type vm:VM_ModSetting}">
                             <Border BorderThickness="1" Margin="0,5" Padding="10" CornerRadius="3">
                                 <Border.Style>
                                     <Style TargetType="Border">
                                         <Setter Property="BorderBrush" Value="DarkGray"/>
                                         <Setter Property="ToolTip" Value="Status: Incomplete (Requires Mugshot Path and/or Mod Data Path assignment)."/>
                                         <Style.Triggers>
                                             <MultiDataTrigger>
                                                 <MultiDataTrigger.Conditions>
                                                     <Condition Binding="{Binding HasMugshotPathAssigned}" Value="True"/>
                                                     <Condition Binding="{Binding HasModPathsAssigned}" Value="True"/>
                                                 </MultiDataTrigger.Conditions>
                                                 <Setter Property="BorderBrush" Value="Green"/>
                                                 <Setter Property="ToolTip" Value="Status: OK (Has Mugshot path and Mod Data path(s))."/>
                                             </MultiDataTrigger>
                                             <MultiDataTrigger>
                                                 <MultiDataTrigger.Conditions>
                                                     <Condition Binding="{Binding HasMugshotPathAssigned}" Value="False"/>
                                                     <Condition Binding="{Binding HasModPathsAssigned}" Value="True"/>
                                                 </MultiDataTrigger.Conditions>
                                                 <Setter Property="BorderBrush" Value="Orange"/>
                                                 <Setter Property="ToolTip" Value="Status: Partial (Has Mod Data path(s), but no Mugshot path assigned)."/>
                                             </MultiDataTrigger>
                                             <MultiDataTrigger>
                                                 <MultiDataTrigger.Conditions>
                                                     <Condition Binding="{Binding HasMugshotPathAssigned}" Value="True"/>
                                                     <Condition Binding="{Binding HasModPathsAssigned}" Value="False"/>
                                                 </MultiDataTrigger.Conditions>
                                                 <Setter Property="BorderBrush" Value="Purple"/>
                                                 <Setter Property="ToolTip" Value="Status: Partial (Has Mugshot path, but no Mod Data path(s) assigned)."/>
                                             </MultiDataTrigger>
                                             <!-- Auto-generated synthetic entries (Base Game / Creation Club) always have
                                                  data: their assets come from the game's BSA archives rather than from
                                                  Corresponding Mod Folder Paths. Treat them as "has data" so they never show
                                                  the purple "mugshot-only" status. Declared after the generic triggers above
                                                  so they win for auto-generated entries. -->
                                             <MultiDataTrigger>
                                                 <MultiDataTrigger.Conditions>
                                                     <Condition Binding="{Binding HasMugshotPathAssigned}" Value="True"/>
                                                     <Condition Binding="{Binding IsAutoGenerated}" Value="True"/>
                                                 </MultiDataTrigger.Conditions>
                                                 <Setter Property="BorderBrush" Value="Green"/>
                                                 <Setter Property="ToolTip" Value="Status: OK (auto-generated entry; mod data comes from the game archives)."/>
                                             </MultiDataTrigger>
                                             <MultiDataTrigger>
                                                 <MultiDataTrigger.Conditions>
                                                     <Condition Binding="{Binding HasMugshotPathAssigned}" Value="False"/>
                                                     <Condition Binding="{Binding IsAutoGenerated}" Value="True"/>
                                                 </MultiDataTrigger.Conditions>
                                                 <Setter Property="BorderBrush" Value="Orange"/>
                                                 <Setter Property="ToolTip" Value="Status: Partial (auto-generated entry; mod data comes from the game archives, but no Mugshot path assigned)."/>
                                             </MultiDataTrigger>
                                              <DataTrigger Binding="{Binding IsMugshotOnlyEntry}" Value="True">
                                                   <Setter Property="ToolTip">
                                                        <Setter.Value>
                                                             <MultiBinding Converter="{StaticResource ModStatusToolTipMultiConverter}"> 
                                                                 <Binding Path="HasMugshotPathAssigned"/>
                                                                 <Binding Path="HasModPathsAssigned"/>
                                                                 <Binding Path="IsMugshotOnlyEntry"/>
                                                             </MultiBinding>
                                                        </Setter.Value>
                                                   </Setter>
                                              </DataTrigger>
                                         </Style.Triggers>
                                     </Style>
                                 </Border.Style>
                                 <Grid>
                                     <Grid.RowDefinitions>
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                         <RowDefinition Height="Auto"/> 
                                     </Grid.RowDefinitions>
                                     <Grid.ColumnDefinitions>
                                         <ColumnDefinition Width="*"/>
                                     </Grid.ColumnDefinitions>
                                     
                                     <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                             <Viewbox Stretch="Uniform" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"
                                                      MaxHeight="25"
                                                      Visibility="{Binding HasPluginWithOverrideRecords, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                 <Viewbox.ToolTip>
                                                     <ToolTip>
                                                         <TextBlock TextWrapping="Wrap">
                                                             This mod has overrides of non-NPC records.
                                                             <LineBreak/>
                                                             NPC2 can handle these using the default option in the settings menu, or on a per-mod basis configured here.
                                                             <LineBreak/>
                                                             <LineBreak/>
                                                             IMPORTANT: Just because the plugin has overrides doesn't mean these overrides need to be handled. In most cases, non-NPC overrides are safe to ignore,
                                                             <LineBreak/>
                                                             and handling them for large plugins can slow down patching considerably. Only handle them if the NPC appearance is bugged otherwise, or you know that it's required.
                                                         </TextBlock>
                                                     </ToolTip>
                                                 </Viewbox.ToolTip>
                                                 <Image Source="/NPC Plugin Chooser 2;component/Resources/Contains Overrides.png" />
                                             </Viewbox>
                                         <TextBlock Text="Refreshing: "
                                                    Foreground="DarkOrange"
                                                    FontWeight="Bold"
                                                    FontSize="14"
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding IsRefreshing, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                             <!-- *** FIX: AncestorType is now ListBox *** -->
                                              <Button Style="{StaticResource LinkButtonStyle}"
                                                      Command="{Binding DataContext.ShowMugshotsCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}"
                                                      CommandParameter="{Binding}"
                                                      ToolTipService.ShowOnDisabled="False">
                                             <Button.ToolTip>
                                                 <MultiBinding Converter="{StaticResource ModSettingLinkButtonTooltipConverter}">
                                                     <Binding Path="HasValidMugshots"/>
                                                     <Binding Path="NpcCount"/>
                                                 </MultiBinding>
                                             </Button.ToolTip>
                                             <TextBlock Text="{Binding DisplayName}" FontWeight="Bold" FontSize="14"/>
                                             </Button>
                                         
                                         <TextBlock Text="{Binding ModKeyDisplaySuffix}"
                                                    FontWeight="Normal" 
                                                    FontSize="12" Margin="5,0,0,0" VerticalAlignment="Center">
                                             <TextBlock.Style>
                                                 <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                                     <Setter Property="Visibility" Value="Visible"/>
                                                     <Setter Property="ToolTip">
                                                         <Setter.Value>
                                                             <ToolTip>
                                                                 <ItemsControl ItemsSource="{Binding CorrespondingModKeys}">
                                                                     <ItemsControl.ItemTemplate>
                                                                         <DataTemplate>
                                                                             <TextBlock>
                                                                                 <TextBlock.Text>
                                                                                     <MultiBinding Converter="{StaticResource ResourceModKeyTooltipConverter}">
                                                                                         <Binding Path="."/>
                                                                                         <Binding Path="DataContext.ResourceOnlyModKeys" RelativeSource="{RelativeSource AncestorType=ItemsControl}"/>
                                                                                     </MultiBinding>
                                                                                 </TextBlock.Text>
                                                                             </TextBlock>
                                                                         </DataTemplate>
                                                                     </ItemsControl.ItemTemplate>
                                                                 </ItemsControl>
                                                             </ToolTip>
                                                         </Setter.Value>
                                                     </Setter>
                                                 </Style>
                                             </TextBlock.Style>
                                         </TextBlock>
                                     </StackPanel>
                                     
                                     <Grid Grid.Row="1" Grid.Column="0" Margin="0,5,0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="Mugshot Folders:" FontWeight="SemiBold" VerticalAlignment="Center" ToolTip="Folder(s) where mugshots for this mod are located."/>
                                        <Button Grid.Column="2" Content="Add" Command="{Binding AddMugshotFolderPathCommand}" Padding="5,1" VerticalAlignment="Center" 
                                                ToolTip="Add a folder where mugshots for this mod are located"/>
                                    </Grid>

                                    <ItemsControl Grid.Row="2" Grid.Column="0" ItemsSource="{Binding MugShotFolderPaths}" Margin="10,0,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,2">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center" ToolTip="{Binding}" TextTrimming="CharacterEllipsis">
                                                        <TextBlock.Foreground>
                                                            <Binding Path="." Converter="{StaticResource PathExistenceToBrushConverter}"/>
                                                        </TextBlock.Foreground>
                                                    </TextBlock>
                                                    <Button Grid.Column="1" Content="Browse..." 
                                                            Command="{Binding DataContext.BrowseMugshotFolderPathCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" 
                                                            CommandParameter="{Binding}" Padding="5,1" Margin="5,0" VerticalAlignment="Center" ToolTip="Select a new folder for this path"/>
                                                    <Button Grid.Column="2" Content="X" Foreground="Red" FontWeight="Bold" 
                                                            Command="{Binding DataContext.RemoveMugshotFolderPathCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" 
                                                            CommandParameter="{Binding}" Padding="5,1" Margin="0,0,5,0" VerticalAlignment="Center" ToolTip="Delete this mugshot folder path"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                     
                                     <Button Grid.Row="3" Grid.Column="0" 
                                             Content="Unlink Mugshot Data" 
                                             Command="{Binding UnlinkMugshotDataCommand}" 
                                             HorizontalAlignment="Left" 
                                             Margin="0,10,0,0" Padding="5,2" 
                                             ToolTip="Separates the assigned mugshot folder into its own entry, if its name differs from the primary mod data folder." 
                                             Visibility="{Binding CanUnlinkMugshots, Converter={StaticResource BooleanToVisibilityConverter}}"/> 

                                     <Grid Grid.Row="4" Grid.Column="0" Margin="0,5,0,5">
                                          <Grid.ColumnDefinitions>
                                              <ColumnDefinition Width="Auto"/>
                                              <ColumnDefinition Width="*"/>
                                              <ColumnDefinition Width="Auto"/>
                                          </Grid.ColumnDefinitions>
                                          <TextBlock Grid.Column="0" Text="Corresponding Mod Folder Paths:" FontWeight="SemiBold" VerticalAlignment="Center" ToolTip="Folder(s) containing plugins and resources (e.g. Meshes, Textures, FaceGen) for this mod."/>
                                          <Button Grid.Column="2" Content="Add" Command="{Binding AddFolderPathCommand}" Padding="5,1" VerticalAlignment="Center" 
                                                  Visibility="{Binding IsAutoGenerated, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}"
                                                  ToolTip="Add another data folder where the contents of this mod are located"/>
                                     </Grid>

                                      <ItemsControl Grid.Row="5" Grid.Column="0" ItemsSource="{Binding CorrespondingFolderPaths}" Margin="10,0,0,0"
                                                    gong:DragDrop.IsDragSource="True"
                                                    gong:DragDrop.IsDropTarget="True"
                                                    x:Name="FolderPathsItemsControl"
                                                    Loaded="FolderPathsItemsControl_Loaded">
                                         <ItemsControl.ItemTemplate>
                                             <DataTemplate>
                                                 <Grid Margin="0,2">
                                                     <Grid.ColumnDefinitions>
                                                         <ColumnDefinition Width="*"/>
                                                         <ColumnDefinition Width="Auto"/>
                                                         <ColumnDefinition Width="Auto"/>
                                                         <ColumnDefinition Width="Auto"/>
                                                     </Grid.ColumnDefinitions>
                                                     <TextBlock Grid.Column="0" Text="{Binding}" VerticalAlignment="Center" ToolTip="{Binding}" TextTrimming="CharacterEllipsis">
                                                         <TextBlock.Foreground>
                                                             <Binding Path="." Converter="{StaticResource PathExistenceToBrushConverter}"/>
                                                         </TextBlock.Foreground>
                                                     </TextBlock>
                                                     <!-- Hidden for the mod's own primary folder: too much of the app keys a mod
                                                          to the folder that shares its name, so repointing it (deliberately or
                                                          by a stray click) would quietly break that correspondence. -->
                                                     <Button Grid.Column="1" Content="Browse..."
                                                             Command="{Binding DataContext.BrowseFolderPathCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                                                             CommandParameter="{Binding}" Padding="5,1" Margin="5,0" VerticalAlignment="Center" ToolTip="Select a data folder for the contents of this mod">
                                                         <Button.Visibility>
                                                             <MultiBinding Converter="{StaticResource NonPrimaryFolderVisibilityConverter}">
                                                                 <Binding Path="."/>
                                                                 <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                                                                 <Binding Path="DataContext.DisplayName" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                                                             </MultiBinding>
                                                         </Button.Visibility>
                                                     </Button>

                                                     <!-- Hidden for the mod's own primary folder: a Refresh rebuilds the list
                                                          around it, so it is never at risk and there is nothing to lock. -->
                                                     <Button Grid.Column="2" Style="{StaticResource FolderLockButtonStyle}"
                                                             Command="{Binding DataContext.ToggleFolderLockCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                                                             CommandParameter="{Binding}">
                                                         <Button.Visibility>
                                                             <MultiBinding Converter="{StaticResource NonPrimaryFolderVisibilityConverter}">
                                                                 <Binding Path="."/>
                                                                 <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                                                                 <Binding Path="DataContext.DisplayName" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                                                             </MultiBinding>
                                                         </Button.Visibility>
                                                     </Button>

                                                     <!-- Hidden for the mod's own primary folder: the next scan re-creates the
                                                          entry around the folder that shares its name, so removing it only
                                                          costs a Refresh to arrive back where it started. -->
                                                     <Button Grid.Column="3" Content="X" Foreground="Red" FontWeight="Bold"
                                                             Command="{Binding DataContext.RemoveFolderPathCommand, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}"
                                                             CommandParameter="{Binding}" Padding="5,1" Margin="0,0,5,0" VerticalAlignment="Center" ToolTip="Delete this data folder">
                                                         <Button.Visibility>
                                                             <MultiBinding Converter="{StaticResource NonPrimaryFolderVisibilityConverter}">
                                                                 <Binding Path="."/>
                                                                 <Binding Path="DataContext" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                                                                 <Binding Path="DataContext.DisplayName" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}"/>
                                                             </MultiBinding>
                                                         </Button.Visibility>
                                                     </Button>
                                                 </Grid>
                                             </DataTemplate>
                                         </ItemsControl.ItemTemplate>
                                     </ItemsControl>
                                     
                                     <WrapPanel Grid.Row="6" Grid.Column="0" Margin="0,5,0,0">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                            <TextBlock Text="Merge Dependencies" 
                                                        VerticalAlignment="Center" 
                                                        ToolTip="{Binding MergeInToolTip}"
                                                        Visibility="{Binding MergeInDependencyRecordsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasAlteredMergeLogic}" Value="True">
                                                                <Setter Property="Foreground" Value="{DynamicResource HighlightAlteredText}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <CheckBox IsChecked="{Binding MergeInDependencyRecords}"
                                                        VerticalAlignment="Center"
                                                        Margin="5 0 0 0"
                                                        ToolTip="{Binding MergeInToolTip}"
                                                        Visibility="{Binding MergeInDependencyRecordsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                            <TextBlock Text="Copy Assets"
                                                        VerticalAlignment="Center"
                                                        ToolTip="If checked, all assets required for the NPC's appearance (meshes, textures, etc.) will be copied to the output folder. If unchecked, only FaceGen files will be copied."/>
                                            <CheckBox IsChecked="{Binding CopyAssets, Mode=TwoWay}"
                                                        VerticalAlignment="Center"
                                                        Margin="5 0 0 0"
                                                        ToolTip="If checked, all assets required for the NPC's appearance (meshes, textures, etc.) will be copied to the output folder. If unchecked, only FaceGen files will be copied."/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0"
                                                    Visibility="{Binding HasBaseGameAssetPaths, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Text="Overwrite Base Game Assets"
                                                        VerticalAlignment="Center"
                                                        ToolTip="{Binding OverwriteBaseGameAssetsToolTip}"/>
                                            <CheckBox IsChecked="{Binding OverwriteBaseGameAssets, Mode=TwoWay}"
                                                        VerticalAlignment="Center"
                                                        Margin="5 0 0 0"
                                                        ToolTip="{Binding OverwriteBaseGameAssetsToolTip}"/>
                                        </StackPanel>

                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0" Visibility="{Binding DataContext.CurrentPatchingMode, ElementName=ModSettingsItemsControl, Converter={StaticResource PatchingModeConverter}, ConverterParameter=CreateAndPatch}">
                                            <TextBlock Text="Include Outfits" 
                                                        VerticalAlignment="Center" 
                                                        ToolTip="If checked, outfits from this mod will be transferred along with NPC appearance when in Create and Patch mode."/>
                                            <CheckBox IsChecked="{Binding IncludeOutfits, Mode=TwoWay}"
                                                        VerticalAlignment="Center"
                                                        Margin="5 0 0 0"
                                                        ToolTip="If checked, outfits from this mod will be transferred along with NPC appearance when in Create and Patch mode."/>
                                        </StackPanel>
                                        
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                                            <TextBlock Text="Handle Injected Records" 
                                                        VerticalAlignment="Center" 
                                                        ToolTip="{Binding HandleInjectedOverridesToolTip}"
                                                        Visibility="{Binding MergeInDependencyRecordsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding HasAlteredHandleInjectedRecordsLogic}" Value="True">
                                                                <Setter Property="Foreground" Value="{DynamicResource HighlightAlteredText}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <CheckBox IsChecked="{Binding HandleInjectedRecords}"
                                                        VerticalAlignment="Center" 
                                                        Margin="5 0 0 0"
                                                        ToolTip="If checked, the patcher will search for and merge in records that are injected. This can potentially make patching take longer."
                                                        Visibility="{Binding MergeInDependencyRecordsVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                            <Button Content="Set Keywords"
                                                    Command="{Binding SetKeywordsCommand}"
                                                    Padding="5,2"
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    ToolTip="{Binding KeywordsToolTip}"/>
                                        </StackPanel>
                                    </WrapPanel>
                                     
                                     <!-- WrapPanel (not StackPanel): with the wig dropdowns shown this row
                                          runs wider than the panel and a StackPanel would clip the last one.
                                          Each label + ComboBox pair is its own inner StackPanel so a wrap
                                          never splits a dropdown from its label — same shape as row 6. -->
                                     <WrapPanel Grid.Row="7" Grid.Column="0" Orientation="Horizontal" Margin="0,5,0,0">
                                       <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,3">
                                         <TextBlock Text="Record Override Handling Mode" VerticalAlignment="Center">
                                             <TextBlock.ToolTip>
                                                 <ToolTip>
                                                     <TextBlock TextWrapping="Wrap" Width="300">
                                                         Controls what happens if plugins in this mod override records from another mod 
                                                         (other than the direct NPC records).
                                                         <LineBreak/>
                                                         <LineBreak/>
                                                         Default: Use the default setting from the Settings Tab.
                                                         <LineBreak/>
                                                         Ignore: Do not look for overridden records. Conflict patching will be fully taken care of by you.
                                                         <LineBreak/>
                                                         Include: Incorporate the overridden records into the output (as whole records in Default mode,
                                                         or deltas in EasyNPC mode).
                                                         <LineBreak/>
                                                         Include as New: Copy the overrides as new records. Avoids conflicts if multiple appearance mods 
                                                         override the same records.
                                                     </TextBlock>
                                                 </ToolTip>
                                             </TextBlock.ToolTip>
                                         </TextBlock>

                                         <!-- SelectedItem (not SelectedValue): WPF can't establish a selection
                                              from a null SelectedValue, so the null-keyed "Default" entry rendered
                                              blank on load. See VM_ModSetting.SelectedRecordOverrideHandlingItem. -->
                                         <ComboBox
                                             ItemsSource="{Binding RecordOverrideHandlingModes}"
                                             SelectedItem="{Binding SelectedRecordOverrideHandlingItem, Mode=TwoWay}"
                                             Margin="5 0 0 0">
                                             <ComboBox.ItemTemplate>
                                                 <DataTemplate>
                                                     <TextBlock Text="{Binding Value}"/>
                                                 </DataTemplate>
                                             </ComboBox.ItemTemplate>
                                         </ComboBox>
                                       </StackPanel>

                                         <!-- Wig Handling Mode: only shown when the analysis scan detected
                                              wigs in this mod (hair-slot wig armors in Default Outfits, or hair
                                              meshes carried in NPC skins/WornArmor) AND wig handling is active
                                              for the current output mode (Create and Patch, or SkyPatcher mode). -->
                                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,3"
                                                     Visibility="{Binding IsWigHandlingVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                             <TextBlock Text="Wig Handling Mode" VerticalAlignment="Center">
                                                 <TextBlock.ToolTip>
                                                     <ToolTip>
                                                         <TextBlock TextWrapping="Wrap" Width="340">
                                                             This mod ships wigs: hair-slot wig armors in its Default Outfits, and/or
                                                             hair meshes carried directly in an NPC's skin (worn armor — the High Poly
                                                             NPC Overhaul pattern). Controls how they are handled during patching — and
                                                             how mugshots and the 3D preview depict the NPC. Antlers are controlled
                                                             separately.
                                                             <LineBreak/><LineBreak/>
                                                             Default: Use the default setting from the Settings tab.
                                                             <LineBreak/>
                                                             Forward To Skin: transfer an outfit wig into the NPC's skin (WornArmor) so
                                                             it shows regardless of the outfit worn (falls back to Forward To Outfit if
                                                             the appearance plugin assigns no WornArmor). A skin-carried wig is already
                                                             in this state and is left as-is.
                                                             <LineBreak/>
                                                             Forward To Outfit: duplicate the outfit the NPC actually wears in game and
                                                             add the wig to the duplicate (no change if it already contains the wig). A
                                                             skin-carried wig is moved into that outfit as a generated wig armor.
                                                             <LineBreak/>
                                                             Convert To Headparts (EXPERIMENTAL): discard the wig armor and make the wig
                                                             the NPC's hair — generated head part records + the wig mesh baked into the
                                                             FaceGen file (HDT-SMP preserved; helmets hide it like normal hair). Works
                                                             for outfit wigs AND skin-carried wigs (the 3D preview's "Set Wig Meshes"
                                                             button corrects which skin meshes count as wigs). An outfit wig falls back
                                                             to Forward To Skin per NPC when the bake would be unsafe; a skin-carried
                                                             wig is left as-is instead.
                                                             <LineBreak/>
                                                             Leave As Is: wigs are treated like any other outfit/skin element — an outfit wig
                                                             is forwarded only if Include Outfits is selected; a skin-carried wig stays
                                                             in the skin.
                                                         </TextBlock>
                                                     </ToolTip>
                                                 </TextBlock.ToolTip>
                                             </TextBlock>

                                             <ComboBox
                                                 ItemsSource="{Binding WigHandlingModes}"
                                                 SelectedItem="{Binding SelectedWigHandlingItem, Mode=TwoWay}"
                                                 Margin="5 0 0 0">
                                                 <ComboBox.ItemTemplate>
                                                     <DataTemplate>
                                                         <TextBlock Text="{Binding Value}"/>
                                                     </DataTemplate>
                                                 </ComboBox.ItemTemplate>
                                             </ComboBox>
                                         </StackPanel>

                                         <!-- Converted Wig Hair Color: same visibility gate as the wig dropdown
                                              above (it is a sub-option of Convert To Headparts). -->
                                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,3"
                                                     Visibility="{Binding IsWigHandlingVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                             <TextBlock Text="Converted Wig Hair Color" VerticalAlignment="Center">
                                                 <TextBlock.ToolTip>
                                                     <ToolTip>
                                                         <TextBlock TextWrapping="Wrap" Width="340">
                                                             Only applies when this mod's Wig Handling Mode resolves to Convert To
                                                             Headparts. Many wig meshes carry a near-black placeholder hair color and
                                                             rely on RaceMenu to recolor them in game (RaceMenu automatically tints worn
                                                             hair-slot items — that is why High Poly NPC Overhaul gives every NPC black
                                                             hair until RaceMenu is installed). A wig baked into the FaceGen file is no
                                                             longer a worn item, so RaceMenu stops recoloring it and the placeholder
                                                             sticks. This bakes the NPC's own hair color in instead.
                                                             <LineBreak/><LineBreak/>
                                                             Default: Use the default setting from the Settings tab.
                                                             <LineBreak/>
                                                             Auto: recolor only wigs carrying a placeholder tint; leave wigs whose
                                                             textures are already colored by their author alone.
                                                             <LineBreak/>
                                                             Always use NPC's hair color: recolor every converted wig, exactly as
                                                             RaceMenu would have done to the original worn wig.
                                                             <LineBreak/>
                                                             Keep the wig's own color: never touch the wig's baked hair color.
                                                         </TextBlock>
                                                     </ToolTip>
                                                 </TextBlock.ToolTip>
                                             </TextBlock>

                                             <ComboBox
                                                 ItemsSource="{Binding WigHairTintModes}"
                                                 SelectedItem="{Binding SelectedWigHairTintItem, Mode=TwoWay}"
                                                 Margin="5 0 0 0">
                                                 <ComboBox.ItemTemplate>
                                                     <DataTemplate>
                                                         <TextBlock Text="{Binding Value}"/>
                                                     </DataTemplate>
                                                 </ComboBox.ItemTemplate>
                                             </ComboBox>
                                         </StackPanel>

                                         <!-- Antler Handling Mode: shown when the analysis scan detected antler
                                              armors / addons / head parts in this mod AND antler handling is active
                                              for the current output mode. Independent of the wig dropdown. -->
                                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,3"
                                                     Visibility="{Binding IsAntlerHandlingVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                             <TextBlock Text="Antler Handling Mode" VerticalAlignment="Center">
                                                 <TextBlock.ToolTip>
                                                     <ToolTip>
                                                         <TextBlock TextWrapping="Wrap" Width="340">
                                                             This mod ships antlers (in an outfit, baked into the worn armor, or as a
                                                             keyword-detected FaceGen head part). Controls how they are handled during
                                                             patching — and how mugshots and the 3D preview depict the NPC.
                                                             <LineBreak/><LineBreak/>
                                                             Default: Use the default setting from the Settings tab.
                                                             <LineBreak/>
                                                             Forward To Skin: transfer the antler into the NPC's skin (WornArmor) so it shows
                                                             regardless of the outfit worn. Falls back to Forward To Outfit if the appearance
                                                             plugin assigns no WornArmor.
                                                             <LineBreak/>
                                                             Forward To Outfit: duplicate the outfit the NPC actually wears in game and add
                                                             the antler to the duplicate.
                                                             <LineBreak/>
                                                             Remove: keep the antler OFF the NPC entirely — stripped from a forwarded outfit,
                                                             from the worn armor, and from the FaceGen. An outfit the load order owns (not
                                                             forwarded) can't be reached.
                                                             <LineBreak/>
                                                             Leave As Is: the antler is treated like any other outfit element — forwarded only if
                                                             Include Outfits is selected.
                                                         </TextBlock>
                                                     </ToolTip>
                                                 </TextBlock.ToolTip>
                                             </TextBlock>

                                             <ComboBox
                                                 ItemsSource="{Binding AntlerHandlingModes}"
                                                 SelectedItem="{Binding SelectedAntlerHandlingItem, Mode=TwoWay}"
                                                 Margin="5 0 0 0">
                                                 <ComboBox.ItemTemplate>
                                                     <DataTemplate>
                                                         <TextBlock Text="{Binding Value}"/>
                                                     </DataTemplate>
                                                 </ComboBox.ItemTemplate>
                                             </ComboBox>
                                         </StackPanel>

                                         <!-- Templated NPCs: only shown when the analysis scan found
                                              Traits-templated NPCs in this mod. Unlike the wig/antler
                                              dropdowns there is no output-mode gate — the mode applies
                                              to record and SkyPatcher output alike. -->
                                         <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,3"
                                                     Visibility="{Binding HasTemplatedNpcs, Converter={StaticResource BooleanToVisibilityConverter}}">
                                             <TextBlock Text="Templated NPCs" VerticalAlignment="Center">
                                                 <TextBlock.ToolTip>
                                                     <ToolTip>
                                                         <TextBlock TextWrapping="Wrap" Width="340">
                                                             Some of this mod's NPCs copy their face from another NPC instead of having
                                                             one of their own (the NPCs tab marks them with a Template icon). Controls
                                                             what a selection from this mod does for those NPCs.
                                                             <LineBreak/><LineBreak/>
                                                             Default: Use the default setting from the Settings tab.
                                                             <LineBreak/>
                                                             Use the template's appearance: they keep showing the other NPC's face, and
                                                             any appearance you pick for them is ignored.
                                                             <LineBreak/>
                                                             Give each NPC its own copy: your choice is applied to each of them
                                                             individually, so NPCs that share a template can look different. Adds one
                                                             copy of the face files per NPC. NPCs whose face the game picks at random
                                                             from a levelled list keep inheriting either way.
                                                         </TextBlock>
                                                     </ToolTip>
                                                 </TextBlock.ToolTip>
                                             </TextBlock>

                                             <ComboBox
                                                 ItemsSource="{Binding TemplateHandlingModes}"
                                                 SelectedItem="{Binding SelectedTemplateHandlingItem, Mode=TwoWay}"
                                                 Margin="5 0 0 0">
                                                 <ComboBox.ItemTemplate>
                                                     <DataTemplate>
                                                         <TextBlock Text="{Binding Value}"/>
                                                     </DataTemplate>
                                                 </ComboBox.ItemTemplate>
                                             </ComboBox>
                                         </StackPanel>
                                     </WrapPanel>

                                     <StackPanel Grid.Row="8" Orientation="Horizontal"
                                                Visibility="{Binding IsOverrideHandlingControlsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <!-- Max Nested Search Layers - hidden when Include All is checked -->
                                        <StackPanel Orientation="Horizontal" 
                                                    Visibility="{Binding IsMaxNestedIntervalDepthVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <TextBlock Text="Max Nested Search Layers"
                                                       VerticalAlignment="Center">
                                                <TextBlock.ToolTip>
                                                    <ToolTip>
                                                        <TextBlock TextWrapping="Wrap" Width="300">
                                                            Controls how many layers deep the patcher will search for overridden records between finding relevant records.
                                                            <LineBreak/><LineBreak/>
                                                            This is not a total depth limit. The count resets every time a record from this mod is found, so a continuous chain of the mod's records is always followed all the way down.
                                                            <LineBreak/><LineBreak/>
                                                            The default of 2 is what lets the search cross one unrelated record (e.g. a vanilla outfit) to reach the mod's edits below it. Higher values may find more obscure overrides but will significantly increase patching time.
                                                            <LineBreak/><LineBreak/>
                                                            Leave blank to use the default. Set to 1 to only check direct dependencies; 0 disables the search entirely.
                                                        </TextBlock>
                                                    </ToolTip>
                                                </TextBlock.ToolTip>
                                            </TextBlock>

                                            <TextBox Text="{Binding MaxNestedIntervalDepth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}"
                                                     MinWidth="40"
                                                     VerticalAlignment="Center"
                                                     Margin="5 0 0 0"
                                                     ToolTip="Default is 2. Leave blank to use default."/>
                                        </StackPanel>
                                        
                                        <!-- Include All checkbox - always visible when this row is visible -->
                                        <StackPanel Orientation="Horizontal" Margin="10,0,0,0">
                                            <TextBlock Text="Include All" 
                                                       VerticalAlignment="Center"
                                                       ToolTip="If checked, grabs ALL override records from the plugins instead of traversing from NPC records. Use with caution."/>
                                            <CheckBox IsChecked="{Binding IncludeAllOverrides, Mode=TwoWay}"
                                                      VerticalAlignment="Center"
                                                      Margin="5,0,0,0"
                                                      ToolTip="If checked, grabs ALL override records from the plugins instead of traversing from NPC records. Use with caution - this may include irrelevant overrides."/>
                                        </StackPanel>

                                        <!-- Which NPC fields the override search starts from. Hidden under
                                             "Include All", which does not traverse from the NPC at all. -->
                                        <Button Content="{Binding OverrideTraversalRootsSummary}"
                                                Command="{Binding SetOverrideTraversalRootsCommand}"
                                                Margin="10,0,0,0" Padding="6,1"
                                                VerticalAlignment="Center"
                                                Visibility="{Binding IsMaxNestedIntervalDepthVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <Button.ToolTip>
                                                <ToolTip>
                                                    <TextBlock TextWrapping="Wrap" Width="320">
                                                        Choose which fields of an NPC record the search for this mod's overridden
                                                        records may start from.
                                                        <LineBreak/><LineBreak/>
                                                        A selected field is followed as deep as it goes, through any kind of record.
                                                        This does not limit which record types can be merged in — only where the
                                                        search begins. Defaults to the fields that carry appearance.
                                                    </TextBlock>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>

                                     <Button Grid.Row="9" Grid.Column="0"
                                             Content="Delete"
                                             Command="{Binding DeleteCommand}"
                                             HorizontalAlignment="Right"
                                             Margin="0,5,0,0" Padding="5,2"
                                             Foreground="Red" FontWeight="Bold"
                                             ToolTip="Permanently delete this empty entry. Only visible if no paths or mugshots are assigned."
                                             Visibility="{Binding CanDelete, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                     
                                     <StackPanel Grid.Row="9" Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left" 
                                                 Margin="0,5,0,0"
                                                 Visibility="{Binding CanDelete, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                                         <Button Content="Set Resource Plugins"
                                                 Command="{Binding SetResourcePluginsCommand}"
                                                 Padding="5,2"
                                                 ToolTip="Select plugins within this mod to be treated as resource files, ignoring their NPCs."/>
    
                                         <Button Command="{Binding RefreshCommand}"
                                                 Margin="5,0,0,0" 
                                                 Padding="5,2"
                                                 Content="Refresh"
                                                 ToolTip="Refresh this mod's data from its source folders."/>
                                     </StackPanel>
                                 </Grid> 
                             </Border> 
                         </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <!-- ============================ VIRTUALIZATION FIX END ============================ -->
            </Grid>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{DynamicResource BorderBrush}" x:Name="ColumnSplitter"/>

            <Grid Grid.Column="2">
                 <Grid.RowDefinitions>
                     <RowDefinition Height="Auto"/> 
                     <RowDefinition Height="*"/>    
                 </Grid.RowDefinitions>

                 <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="0,0,0,1" Padding="5" MinHeight="30">
                     <Grid>
                          <TextBlock Text="{Binding SelectedModForMugshots.DisplayName, StringFormat='Mugshots for: {0}', TargetNullValue='Select a mod from the left panel'}"
                                     FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" HorizontalAlignment="Left" TextTrimming="CharacterEllipsis"/>
                          <Button Content="Cancel Mugshot Load"
                                  Command="{Binding CancelMugshotLoadCommand}"
                                  VerticalAlignment="Center" HorizontalAlignment="Right"
                                  Padding="5,2"
                                  Visibility="{Binding IsLoadingMugshots, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                     </Grid>
                 </Border>

                 <ScrollViewer Grid.Row="1" x:Name="MugshotScrollViewer"
                               VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                               SizeChanged="MugshotScrollViewer_SizeChanged"
                               PreviewMouseWheel="MugshotScrollViewer_PreviewMouseWheel">
                     <ItemsControl x:Name="MugshotsItemsControl" ItemsSource="{Binding CurrentModNpcMugshots}">
                         <ItemsControl.ItemsPanel>
                             <ItemsPanelTemplate>
                                 <WrapPanel Orientation="Horizontal"
                                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"/>
                             </ItemsPanelTemplate>
                         </ItemsControl.ItemsPanel>
                         <ItemsControl.ItemTemplate>
                             <DataTemplate DataType="{x:Type vm:VM_ModsMenuMugshot}">
                                <Border BorderThickness="2" BorderBrush="{Binding BorderColor}" Margin="5" CornerRadius="2"
                                        Width="{Binding ImageWidth}" Height="{Binding ImageHeight}"
                                        PreviewMouseRightButtonDown="MugshotItem_PreviewMouseRightButtonDown"
                                        ToolTip="{Binding ToolTipText}">
                                    
                                    <Grid Background="Black">
                                        <Image Source="{Binding MugshotSource}" Stretch="UniformToFill" UseLayoutRounding="True" SnapsToDevicePixels="True">
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="Opacity" Value="1.0" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasMugshot}" Value="False">
                                                            <Setter Property="Opacity" Value="0.3"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                        
                                        <Image x:Name="LoadingDragonImage"
                                               Source="/NPC Plugin Chooser 2;component/Resources/RedDragon.png"
                                               Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               RenderTransformOrigin="0.5,0.5"
                                               Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource PercentageSizingConverter}, ConverterParameter='50'}"
                                               Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                            
                                            <Image.Style>
                                                <Style TargetType="Image">
                                                    <Setter Property="RenderTransform">
                                                        <Setter.Value>
                                                            <TransformGroup>
                                                                <TranslateTransform Y="0" /> <RotateTransform Angle="0" />
                                                            </TransformGroup>
                                                        </Setter.Value>
                                                    </Setter>

                                                    <Setter Property="local:DynamicRadiusBehavior.ImageWidth" Value="{Binding ImageWidth}" />
                                                    <Setter Property="local:DynamicRadiusBehavior.ImageHeight" Value="{Binding ImageHeight}" />
                                                    <Setter Property="local:DynamicRadiusBehavior.RadiusMultiplier" Value="0.25" />

                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                            <DataTrigger.EnterActions>
                                                                <BeginStoryboard x:Name="OrbitDragon_BeginStoryboard">
                                                                    <Storyboard>
                                                                        <DoubleAnimation
                                                                            Storyboard.TargetProperty="RenderTransform.Children[1].Angle"
                                                                            From="0" To="360" Duration="0:0:3"
                                                                            RepeatBehavior="Forever" />
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </DataTrigger.EnterActions>
                                                            <DataTrigger.ExitActions>
                                                                <StopStoryboard BeginStoryboardName="OrbitDragon_BeginStoryboard"/>
                                                            </DataTrigger.ExitActions>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Image.Style>
                                        </Image>
                                        
                                        <Border VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Padding="3,1"
                                                Background="{DynamicResource MugshotCaptionBackground}">
                                            <TextBlock Text="{Binding NpcDisplayName}" Foreground="{DynamicResource MugshotCaptionForeground}" FontSize="10"
                                                       TextTrimming="CharacterEllipsis" HorizontalAlignment="Center"/>
                                        </Border>

                                        <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0 0 0 10"
                                              Visibility="{Binding IsAmbiguousSource, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              ToolTip="This NPC is defined in multiple source plugins. Right-click to select the correct source."
                                              Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource PercentageSizingConverter}, ConverterParameter='20'}"
                                              Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                            <Viewbox Stretch="Uniform">
                                                <Image Source="/NPC Plugin Chooser 2;component/Resources/Multiple Plugins for NPC.png" />
                                            </Viewbox>
                                        </Grid>

                                        <!-- Missing Asset Indicator (Top-Center) — meshes OR textures -->
                                        <Grid HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0 0 0 0"
                                              Visibility="{Binding HasMissingAssets, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              ToolTip="{Binding MissingAssetNotificationText}"
                                              Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource PercentageSizingConverter}, ConverterParameter='20'}"
                                              Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                            <Viewbox Stretch="Uniform">
                                                <Image Source="/NPC Plugin Chooser 2;component/Resources/Missing Asset.png" />
                                            </Viewbox>
                                        </Grid>

                                        <!-- Missing-outfit-assets indicator (Top-Right): the outfit/
                                             headgear is missing meshes or textures (installable —
                                             re-render picks them up), and/or an outfit mesh links an
                                             SMP/HDT physics XML that doesn't exist (informational — the
                                             render is correct). Separate from the base missing-asset
                                             icon above. -->
                                        <Grid HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0 0 0 0"
                                              Visibility="{Binding HasMissingOutfitAssets, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              ToolTip="{Binding MissingOutfitAssetsText}"
                                              Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource PercentageSizingConverter}, ConverterParameter='20'}"
                                              Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                            <Viewbox Stretch="Uniform">
                                                <Image Source="/NPC Plugin Chooser 2;component/Resources/Missing Outfit Assets.png" />
                                            </Viewbox>
                                        </Grid>

                                        <!-- Data-folder-assets indicator (Top-Left): the render pulled
                                             non-vanilla assets from the data folder because they weren't
                                             in this mod's Corresponding Mod Folders — a keep-activated
                                             runtime dependency. Informational; tooltip lists the assets.
                                             White halo on the Viewbox so the glyph doesn't camouflage. -->
                                        <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0 0 0 0"
                                              Visibility="{Binding HasDataFolderAssets, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              ToolTip="{Binding DataFolderAssetsText}"
                                              Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource PercentageSizingConverter}, ConverterParameter='20'}"
                                              Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                            <Viewbox Stretch="Uniform">
                                                <Viewbox.Effect>
                                                    <DropShadowEffect Color="White" ShadowDepth="0"
                                                                      BlurRadius="6" Opacity="0.9"/>
                                                </Viewbox.Effect>
                                                <TextBlock Text="&#xE8B7;" FontFamily="Segoe MDL2 Assets" Foreground="DodgerBlue"/>
                                            </Viewbox>
                                        </Grid>

                                        <!-- Outfit-conflict indicator (Bottom-Left): "Include Outfit" is
                                             overridden at runtime by SkyPatcher/SPID, or NPC2's SkyPatcher
                                             ini entry is not conflict-winning. Tooltip explains why. -->
                                        <Grid HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="0 0 0 10"
                                              Visibility="{Binding HasOutfitNotice, Converter={StaticResource BooleanToVisibilityConverter}}"
                                              ToolTip="{Binding OutfitNoticeText}"
                                              Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource PercentageSizingConverter}, ConverterParameter='20'}"
                                              Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                            <Viewbox Stretch="Uniform">
                                                <TextBlock Text="⚠" FontWeight="Bold" Foreground="Orange"/>
                                            </Viewbox>
                                        </Grid>

                                    </Grid>
                                    
                                    <Border.ContextMenu>
                                        <ContextMenu> 
                                               <MenuItem  Header="Jump to NPC in List" Command="{Binding JumpToNpcCommand}"/> 
                                               <MenuItem Header="View Full Screen (Ctrl+RClick)" Command="{Binding ToggleFullScreenCommand}"/>
                                               <MenuItem Header="Show 3D Preview (Ctrl+Shift+RClick)" Command="{Binding Show3DPreviewCommand}"/>
                                               <Separator />
                                               <MenuItem Command="{Binding AddToFavoritesCommand}"> 
                                                   <MenuItem.Style> 
                                                       <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}"> 
                                                           <Setter Property="Header" Value="Add to Favorites"/> 
                                                           <Style.Triggers> 
                                                               <DataTrigger Binding="{Binding IsFavorite}" Value="True"> 
                                                                   <Setter Property="Header" Value="Remove from Favorites"/> 
                                                               </DataTrigger> 
                                                           </Style.Triggers> 
                                                       </Style> 
                                                   </MenuItem.Style> 
                                               </MenuItem> 
                                               <MenuItem Header="Select Source Plugin" 
                                                         Visibility="{Binding IsAmbiguousSource, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                                         ItemsSource="{Binding AvailableSourcePlugins}"> 
                                                   <MenuItem.ItemContainerStyle> 
                                                       <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}"> 
                                                           <Setter Property="Header" Value="{Binding Converter={StaticResource ModKeyToStringConverter}}"/> 
                                                           <Setter Property="Command" Value="{Binding DataContext.SetNpcSourcePluginCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/> 
                                                           <Setter Property="CommandParameter" Value="{Binding}"/> 
                                                           <Setter Property="IsCheckable" Value="True"/> 
                                                           <Setter Property="IsChecked"> 
                                                               <Setter.Value> 
                                                                   <MultiBinding Converter="{StaticResource AreModKeysEqualMultiConverter}"> 
                                                                       <!-- AreModKeysEqualMultiConverter checks if the current menu item's ModKey (value[0]) 
                                                                            equals the VM_ModsMenuMugshot's CurrentSourcePlugin (value[1]) --> 
                                                                       <Binding Path="."/> <!-- The ModKey for this MenuItem --> 
                                                                       <Binding Path="DataContext.CurrentSourcePlugin" RelativeSource="{RelativeSource AncestorType=ContextMenu}"/> 
                                                                   </MultiBinding> 
                                                               </Setter.Value> 
                                                           </Setter> 
                                                       </Style> 
                                                   </MenuItem.ItemContainerStyle> 
                                               </MenuItem> 
                                               <MenuItem Header="Select Same Source Plugin Where Possible" 
                                                         Visibility="{Binding IsAmbiguousSource, Converter={StaticResource BooleanToVisibilityConverter}}" 
                                                         Command="{Binding SelectSameSourcePluginWherePossibleCommand}" 
                                                         ToolTip="For all other ambiguous NPCs in this mod, sets their source to this source plugin if it contains the NPC."/> 
                                               <Separator/>
                                               <MenuItem Header="Open Mod Folder" 
                                                  ItemsSource="{Binding ParentVMModSetting.CorrespondingFolderPaths}"
                                                  Command="{Binding OpenFolderCommand}"
                                                  CommandParameter="{Binding ParentVMModSetting.CorrespondingFolderPaths[0]}"
                                                  Visibility="{Binding CanOpenModFolder, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding ParentVMModSetting.CorrespondingFolderPaths.Count}" Value="1">
                                                            <Setter Property="ItemsSource" Value="{x:Null}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding ParentVMModSetting.CorrespondingFolderPaths.Count, Converter={StaticResource IsGreaterThanOneConverter}}" Value="True">
                                                            <Setter Property="Command" Value="{x:Null}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                            <MenuItem.ItemContainerStyle>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Setter Property="Header" Value="{Binding Converter={StaticResource PathToFolderNameConverter}}"/>
                                                    <Setter Property="Command" Value="{Binding DataContext.OpenFolderCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                    <Setter Property="CommandParameter" Value="{Binding}"/>
                                                </Style>
                                            </MenuItem.ItemContainerStyle>
                                        </MenuItem>
                                        <MenuItem Header="Open Mugshot Folder" 
                                                  Command="{Binding OpenFolderCommand}"
                                                  CommandParameter="{Binding MugshotFolderPath}"
                                                  Visibility="{Binding CanOpenMugshotFolder, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                               <MenuItem Header="Visit Mod Page"
                                                         DisplayMemberPath="DisplayName"
                                                         Visibility="{Binding CanVisitModPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                   <MenuItem.Style>
                                                       <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                           <Setter Property="Command" Value="{Binding VisitModPageCommand}"/>
                                                           <Setter Property="CommandParameter" Value="{Binding ModPageUrls[0].Url}"/>
                                                           <Style.Triggers>
                                                               <DataTrigger Binding="{Binding HasSingleModPage}" Value="False">
                                                                   <Setter Property="Command" Value="{x:Null}"/>
                                                                   <Setter Property="CommandParameter" Value="{x:Null}"/>
                                                                   <Setter Property="ItemsSource" Value="{Binding ModPageUrls}"/>
                                                               </DataTrigger>
                                                           </Style.Triggers>
                                                       </Style>
                                                   </MenuItem.Style>
                                                   <MenuItem.ItemContainerStyle>
                                                       <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                           <Setter Property="Command" Value="{Binding DataContext.VisitModPageCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                           <Setter Property="CommandParameter" Value="{Binding Url}"/>
                                                       </Style>
                                                   </MenuItem.ItemContainerStyle>
                                               </MenuItem>
                                        </ContextMenu> 

                                    </Border.ContextMenu>
                                </Border>
                            </DataTemplate>
                         </ItemsControl.ItemTemplate>
                     </ItemsControl>
                 </ScrollViewer>

                  <TextBlock Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14" Foreground="Gray" TextWrapping="Wrap" Margin="20">
                      <TextBlock.Style>
                           <Style TargetType="TextBlock">
                              <Setter Property="Visibility" Value="Collapsed"/> 
                              <Style.Triggers>
                                  <DataTrigger Binding="{Binding SelectedModForMugshots}" Value="{x:Null}">
                                       <Setter Property="Visibility" Value="Visible"/>
                                       <Setter Property="Text" Value="Click a mod name link in the left panel to view its NPC mugshots."/>
                                  </DataTrigger>
                                  <MultiDataTrigger>
                                      <MultiDataTrigger.Conditions>
                                          <Condition Binding="{Binding SelectedModForMugshots, Converter={StaticResource IsNotNullConverter}}" Value="True"/>
                                          <Condition Binding="{Binding IsLoadingMugshots}" Value="False"/>
                                          <Condition Binding="{Binding CurrentModNpcMugshots.Count}" Value="0"/>
                                      </MultiDataTrigger.Conditions>
                                      <Setter Property="Visibility" Value="Visible"/>
                                      <Setter Property="Text" Value="No valid mugshot images found in the specified folder for the selected mod, or the folder path is invalid/inaccessible."/>
                                  </MultiDataTrigger>
                                  <DataTrigger Binding="{Binding IsLoadingMugshots}" Value="True">
                                      <Setter Property="Visibility" Value="Collapsed"/>
                                  </DataTrigger>
                              </Style.Triggers>
                          </Style>
                      </TextBlock.Style>
                  </TextBlock>
            </Grid>
        </Grid>
        
        <!-- Bottom Control Row -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="5">
            <DockPanel LastChildFill="True">

                <GroupBox Header="Zoom Controls" DockPanel.Dock="Right" Margin="10,2,0,2" Padding="5,2">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Button x:Name="ZoomOutButtonMods" Content="-" Width="25" Height="25" Margin="2,0" ToolTip="Zoom Out (Ctrl+Scroll Wheel)"/>
                        <TextBox x:Name="ZoomPercentageTextBoxMods" Width="60" Height="25" Margin="2,0" TextAlignment="Center" VerticalContentAlignment="Center"
                                 Text="{Binding ModsViewZoomLevel, StringFormat='F2', Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PreviewMouseWheel="ZoomPercentageTextBoxMods_PreviewMouseWheel"
                                 ToolTip="Current Zoom Level. Editable. Ctrl+Scroll Wheel in image area or Scroll Wheel here to adjust."/>
                        <TextBlock Text="%" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <Button x:Name="ZoomInButtonMods" Content="+" Width="25" Height="25" Margin="2,0" ToolTip="Zoom In (Ctrl+Scroll Wheel)"/>
                        <CheckBox x:Name="LockZoomCheckBoxMods" Content="Lock Zoom" Margin="10,0,5,0" VerticalAlignment="Center" ToolTip="Lock the current zoom level. If unchecked, zoom adjusts to fit images."/>
                        <Button x:Name="ResetZoomModsButton" Content="Reset Zoom" Margin="5,0,0,0" Padding="5,2"
                                Command="{Binding ResetZoomModsCommand}"
                                ToolTip="Reset zoom to automatically fit images."/>
                    </StackPanel>
                </GroupBox>

                <WrapPanel VerticalAlignment="Center">
                    <TextBlock Text="Batch Actions:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,2,5,2"/>

                    <GroupBox Header="Refresh" Margin="5,2" Padding="5,2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Refresh All" Command="{Binding RefreshAllModsCommand}" Padding="5,2" ToolTip="Completely re-scans all mod folders and rebuilds the mod list."/>
                            <CheckBox Content="Rescan Non-Appearance Mods"
                                      IsChecked="{Binding ShouldRescanNonAppearanceMods, Mode=TwoWay}"
                                      VerticalAlignment="Center"
                                      Margin="5,0,0,0"
                                      ToolTip="If checked, the 'Refresh All' action will also clear the cache of non-appearance mods and attempt to re-import them."/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Outfits" Margin="5,2" Padding="5,2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Include" Command="{Binding BatchIncludeOutfitsCommand}" Margin="0,0,5,0" Padding="5,2" ToolTip="Enable 'Include Outfits' for ALL mods."/>
                            <Button Content="Exclude" Command="{Binding BatchExcludeOutfitsCommand}" Padding="5,2" ToolTip="Disable 'Include Outfits' for ALL mods."/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Injected Record Search" Margin="5,2" Padding="5,2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Enable" Command="{Binding BatchEnableInjectedRecordsCommand}" Margin="0,0,5,0" Padding="5,2" ToolTip="Enable 'Search for Injected Records' for ALL mods."/>
                            <Button Content="Disable" Command="{Binding BatchDisableInjectedRecordsCommand}" Padding="5,2" ToolTip="Disable 'Search for Injected Records' for ALL mods."/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Merge-in" Margin="5,2" Padding="5,2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="On" Command="{Binding BatchEnableMergeInCommand}" Margin="0,0,5,0" Padding="5,2" ToolTip="Enable 'Merge Dependencies' for all mods not flagged as non-appearance."/>
                            <Button Content="Force" Command="{Binding BatchForceEnableMergeInCommand}" Margin="0,0,5,0" Padding="5,2" ToolTip="Enable 'Merge Dependencies' for ALL mods (use with caution)."/>
                            <Button Content="Off" Command="{Binding BatchDisableMergeInCommand}" Padding="5,2" ToolTip="Disable 'Merge Dependencies' for ALL mods."/>
                        </StackPanel>
                    </GroupBox>

                    <GroupBox Header="Asset Copying" Margin="5,2" Padding="5,2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button Content="Enable" Command="{Binding BatchEnableCopyAssetsCommand}" Margin="0,0,5,0" Padding="5,2" ToolTip="Enable 'Copy Assets' for ALL mods."/>
                            <Button Content="Disable" Command="{Binding BatchDisableCopyAssetsCommand}" Padding="5,2" ToolTip="Disable 'Copy Assets' for ALL mods."/>
                        </StackPanel>
                    </GroupBox>
                    
                    <GroupBox Header="CotR" Margin="5,2" Padding="5,2">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Keyword:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBox Text="{Binding CotRKeyword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                     Width="80" VerticalAlignment="Center" Margin="0,0,5,0"
                                     ToolTip="The keyword that will be applied to Charmers of the Reach mods and used to identify them for RSV exclusion."/>
                            <Button Content="Auto-Apply CotR Keyword" 
                                    Command="{Binding ApplyCotRKeywordCommand}" 
                                    Margin="0,0,5,0" Padding="5,2" 
                                    ToolTip="Reads CotR_Plugins.txt from the application directory and applies the CotR keyword to all mod settings containing a matching plugin. The default list includes many popular CotR-based replacers but can be edited manually."/>
                            <Button Content="Write RSV Exclusion" 
                                    Command="{Binding WriteRsvExclusionCommand}" 
                                    Padding="5,2" 
                                    ToolTip="Adds the 'RSVignore' keyword to all mod settings that have the CotR keyword, preventing RSV from overwriting their skin textures."/>
                        </StackPanel>
                    </GroupBox>
                    
                    <GroupBox Margin="5,2" Padding="5,2"
                              Visibility="{Binding ShowSourcePluginControls, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="Source Plugin for Ambiguous NPCs:" VerticalAlignment="Center" Margin="0,0,5,0"
                                       ToolTip="Select a plugin to be the default source for NPCs found in multiple plugins within this mod setting."/>
                            <ComboBox ItemsSource="{Binding AvailableSourcePluginsForSelectedMod}" 
                                      SelectedItem="{Binding SelectedSourcePluginForDisambiguation, Mode=TwoWay}"
                                      DisplayMemberPath="FileName"
                                      MinWidth="150" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <Button Content="Set as Source Plugin" Command="{Binding SetGlobalSourcePluginCommand}" 
                                    Padding="5,2" VerticalAlignment="Center"
                                    ToolTip="Set the selected plugin as the primary source for all NPCs in this mod setting that are found in multiple plugins."/>
                        </StackPanel>
                    </GroupBox>
                </WrapPanel>
            </DockPanel>
        </Border>
    </Grid>
</reactiveui:ReactiveUserControl>
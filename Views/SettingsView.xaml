<!-- SettingsView.xaml -->
<reactiveui:ReactiveUserControl x:Class="NPC_Plugin_Chooser_2.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NPC_Plugin_Chooser_2.Views"
             xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
             xmlns:bsky="clr-namespace:Mutagen.Bethesda.Skyrim;assembly=Mutagen.Bethesda.Skyrim"
             xmlns:models="clr-namespace:NPC_Plugin_Chooser_2.Models"
             xmlns:plugins="clr-namespace:Mutagen.Bethesda.WPF.Plugins;assembly=Mutagen.Bethesda.WPF"
             xmlns:reactiveui="http://reactiveui.net"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:dd="urn:gong-wpf-dragdrop"
             xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             x:TypeArguments="vm:VM_Settings"
             d:DataContext="{d:DesignInstance Type=vm:VM_Settings}"
             d:DesignHeight="800" d:DesignWidth="800"> <!-- Increased height for more controls -->
    <UserControl.Resources>
        <local:FullPathToFolderNameConverter x:Key="FullPathToFolderNameConverter"/>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:BooleanToOpacityConverter x:Key="BooleanToOpacityConverter"/>
        <local:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
        <local:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
        <ObjectDataProvider x:Key="SkyrimReleaseEnumValues" MethodName="GetValues" ObjectType="{x:Type System:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="bsky:SkyrimRelease"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <!-- Optional: Converter for Enum descriptions if desired -->

        <!-- Section frame for this page. Each section used to be a GroupBox; the Expander
             keeps the same framed look but turns the caption into a click target, so the
             page collapses down to its headers. IsExpanded is bound per-section to a
             VM_SettingsSection, which persists the state to Settings.json.

             Templated here rather than per theme: only two of the four themes retemplate
             GroupBox, and every brush this needs exists in all four, so one DynamicResource
             style follows runtime theme swaps without four near-identical copies. -->
        <Style x:Key="SettingsSectionExpander" TargetType="Expander">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <DockPanel>
                                <ToggleButton x:Name="HeaderSite" DockPanel.Dock="Top"
                                              Content="{TemplateBinding Header}"
                                              ContentTemplate="{TemplateBinding HeaderTemplate}"
                                              ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                              IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Foreground="{TemplateBinding Foreground}"
                                              FontWeight="SemiBold"
                                              Cursor="Hand"
                                              HorizontalContentAlignment="Stretch"
                                              ToolTip="Click to show or hide this section. Sections stay as you leave them next time you launch.">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="HeaderBorder"
                                                    Background="{DynamicResource TertiaryBackground}"
                                                    CornerRadius="3,3,0,0"
                                                    Padding="7,5">
                                                <StackPanel Orientation="Horizontal">
                                                    <!-- A vector chevron rather than a glyph: Segoe UI has no
                                                         matched pair of triangles (U+25BC exists, U+25B6 does
                                                         not), so text would font-fall-back. Same shape the
                                                         NPCs view's collapsible group headers use. -->
                                                    <Path x:Name="Chevron"
                                                          Data="F1 M 0,0 L 5,0 L 2.5,3 Z"
                                                          Stretch="Uniform" Width="8" Height="8"
                                                          Fill="{TemplateBinding Foreground}"
                                                          VerticalAlignment="Center" Margin="0,0,6,0"
                                                          RenderTransformOrigin="0.5,0.5"/>
                                                    <ContentPresenter VerticalAlignment="Center"
                                                                      RecognizesAccessKey="False"/>
                                                </StackPanel>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="False">
                                                    <Setter TargetName="Chevron" Property="RenderTransform">
                                                        <Setter.Value>
                                                            <RotateTransform Angle="-90"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <!-- No content below it to meet, so the collapsed header
                                                         rounds off on all four corners. -->
                                                    <Setter TargetName="HeaderBorder" Property="CornerRadius" Value="3"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="HeaderBorder" Property="Background"
                                                            Value="{DynamicResource SelectedItemBackground}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <Border x:Name="ContentSite"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="0,1,0,0"
                                        Visibility="Collapsed">
                                    <ContentPresenter Margin="{TemplateBinding Padding}" Focusable="False"/>
                                </Border>
                            </DockPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
        <StackPanel>

            <!-- Section 1: Game Environment -->
            <Expander Header="Game Environment" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionGameEnvironment.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <!-- Skyrim Game Path -->
                    <TextBlock Text="Skyrim Game Data Path (Optional - Auto-detects if blank):" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding SkyrimGamePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectGameFolderCommand}"/>
                    </Grid>
                    <TextBlock Text="Leave blank to attempt auto-detection based on the selected Skyrim version." FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,10"/>

                    <!-- Skyrim Version -->
                    <TextBlock Text="Skyrim Version:" Margin="0,0,0,2"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource SkyrimReleaseEnumValues}}"
                              SelectedItem="{Binding SkyrimRelease, Mode=TwoWay}" Margin="0,0,0,2"/>
                    <TextBlock Text="Ensure this matches the game version you are modding." FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,10"/>

                    <!-- Environment Status -->
                    <TextBlock Text="Environment Status:" FontWeight="Bold" Margin="0,5,0,2"/>
                    <TextBlock>
                        <Run Text="Mutagen Environment Status: "/>
                        <Run FontWeight="Bold">
                            <Run.Style>
                                <Style TargetType="Run">
                                    <Setter Property="Text" Value="{Binding EnvironmentStatus, Mode=OneWay}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding EnvironmentStatus}" Value="Valid">
                                            <Setter Property="Foreground" Value="{DynamicResource StatusValidForeground}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding EnvironmentStatus}" Value="Invalid">
                                            <Setter Property="Foreground" Value="{DynamicResource StatusErrorForeground}"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding EnvironmentStatus}" Value="Pending">
                                            <Setter Property="Foreground" Value="{DynamicResource StatusPendingForeground}"/>
                                        </DataTrigger>
                                        <!-- Valid, but the load order holds no mod plugins at all. Declared
                                             last so it overrides the plain Valid trigger above. -->
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding EnvironmentStatus}" Value="Valid"/>
                                                <Condition Binding="{Binding EnvironmentLoadOrderIsVanillaOnly}" Value="True"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Foreground" Value="{DynamicResource StatusWarningForeground}"/>
                                            <Setter Property="Text" Value="Valid (no mod plugins found)"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Run.Style>
                        </Run>
                        <Run Text=" - "/>
                        <Run FontSize="10">
                            <Run.Text>
                                <MultiBinding StringFormat="{}{0} Plugins in Load Order ({1} Active)">
                                    <Binding Path="NumPluginsInEnvironment" Mode="OneWay" />
                                    <Binding Path="NumActivePluginsInEnvironment" Mode="OneWay" />
                                </MultiBinding>
                            </Run.Text>
                        </Run>
                    </TextBlock>
                    <!-- Advisory (not an error - the environment is usable): the resolved load order
                         contains only base-game / Creation Club plugins. Nearly always means N.P.C.2 was
                         launched outside the mod manager, so Mutagen read Windows' own Plugins.txt rather
                         than the manager's profile. Appearance mods are still found (those are scanned
                         straight from the Mods folder); what breaks is everything downstream of the load
                         order - patching against the right conflict winners, and the NPCs menu, which
                         only ever sees base-game / CC NPCs. Detail lives in the tooltip. -->
                    <TextBlock TextWrapping="Wrap" Margin="0,4,0,0"
                               ToolTipService.ShowDuration="60000"
                               Foreground="{DynamicResource StatusWarningForeground}"
                               Text="Your load order contains only base game and Creation Club plugins. Relaunch N.P.C.2 through your mod manager, and check that the Skyrim Game Data Path above matches the one set in your mod manager's settings.">
                        <TextBlock.ToolTip>
                            <ToolTip>
                                <TextBlock TextWrapping="Wrap" MaxWidth="460"
                                           Text="N.P.C.2 reads the load order from Windows' own Plugins.txt (path shown below). Mod managers such as MO2 and Vortex redirect that file to the active profile's load order, but only for programs launched through them - otherwise the Skyrim launcher's own list is used instead.&#10;&#10;Your appearance mods are still detected, since those are read directly from the Mods folder. What breaks is everything that depends on the load order: the output plugin will not be patched against the correct conflict winners, and NPCs from anything other than the base game and Creation Club will not appear in the NPCs menu."/>
                            </ToolTip>
                        </TextBlock.ToolTip>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding EnvironmentStatus}" Value="Valid"/>
                                            <Condition Binding="{Binding EnvironmentLoadOrderIsVanillaOnly}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Text="{Binding EnvironmentErrorText}" Foreground="{DynamicResource StatusErrorForeground}" TextWrapping="Wrap" MaxHeight="100" ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="{x:Null}"><Setter Property="Visibility" Value="Collapsed"/></Trigger>
                                    <Trigger Property="Text" Value=""><Setter Property="Visibility" Value="Collapsed"/></Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <TextBlock Margin="0,2,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding EnvironmentErrorText}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding EnvironmentErrorText}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <Hyperlink Command="{Binding ShowFullEnvironmentErrorCommand}">
                            <Run Text="Click to display whole error" FontSize="11"/>
                        </Hyperlink>
                    </TextBlock>

                    <!-- Detailed environment paths / CC detection diagnostics.
                         Useful for users on non-standard installs (renamed folders,
                         moved drives) where Mutagen's default game-folder lookup
                         can't find Skyrim.ccc and CC plugins go missing from the
                         load order. -->
                    <StackPanel Margin="0,8,0,0">
                        <TextBlock FontSize="10" Foreground="{DynamicResource SecondaryForeground}" TextWrapping="Wrap">
                            <Run Text="Data Folder: " FontWeight="Bold"/>
                            <Run Text="{Binding EnvironmentDataFolderPath, Mode=OneWay}"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{DynamicResource SecondaryForeground}" TextWrapping="Wrap">
                            <Run Text="plugins.txt: " FontWeight="Bold"/>
                            <Run Text="{Binding EnvironmentLoadOrderFilePath, Mode=OneWay}"/>
                            <Run Text=" ("/>
                            <Run>
                                <Run.Style>
                                    <Style TargetType="Run">
                                        <Setter Property="Text" Value="missing"/>
                                        <Setter Property="Foreground" Value="{DynamicResource StatusErrorForeground}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EnvironmentLoadOrderFileExists}" Value="True">
                                                <Setter Property="Text" Value="exists"/>
                                                <Setter Property="Foreground" Value="{DynamicResource StatusValidForeground}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Run.Style>
                            </Run>
                            <Run Text=")"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{DynamicResource SecondaryForeground}" TextWrapping="Wrap">
                            <Run Text="Skyrim.ccc: " FontWeight="Bold"/>
                            <Run Text="{Binding EnvironmentCreationClubListingsFilePath, Mode=OneWay}"/>
                            <Run Text=" ("/>
                            <Run>
                                <Run.Style>
                                    <Style TargetType="Run">
                                        <Setter Property="Text" Value="not found"/>
                                        <Setter Property="Foreground" Value="{DynamicResource StatusErrorForeground}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding EnvironmentCreationClubListingsSource}" Value="Mutagen">
                                                <Setter Property="Text" Value="from Mutagen"/>
                                                <Setter Property="Foreground" Value="{DynamicResource StatusValidForeground}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding EnvironmentCreationClubListingsSource}" Value="Fallback">
                                                <Setter Property="Text" Value="from fallback probe"/>
                                                <Setter Property="Foreground" Value="{DynamicResource StatusPendingForeground}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Run.Style>
                            </Run>
                            <Run Text=")"/>
                        </TextBlock>
                        <TextBlock FontSize="10" Foreground="{DynamicResource SecondaryForeground}">
                            <Run>
                                <Run.Text>
                                    <MultiBinding StringFormat="{}CC plugins detected: {0} (of which {1} are in the resolved load order)">
                                        <Binding Path="EnvironmentCreationClubPluginsCount" Mode="OneWay"/>
                                        <Binding Path="EnvironmentCreationClubPluginsInLoadOrderCount" Mode="OneWay"/>
                                    </MultiBinding>
                                </Run.Text>
                            </Run>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </Expander>

            <!-- Section 2: Mod Environment -->
            <Expander Header="Mod Environment" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionModEnvironment.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <TextBlock Text="Mods Folder (e.g., MO2 Mods Path):" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding ModsFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectModsFolderCommand}"/>
                    </Grid>
                    <TextBlock Text="Specify the root folder where your mods are installed (used for Mod linking)."
                               FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,10"/>

                    <!-- MO2 Modlist Filter -->
                    <CheckBox Content="Filter by active mods (MO2)"
                              IsChecked="{Binding FilterByActiveModsMO2, Mode=TwoWay}"
                              ToolTip="When enabled, only mod folders corresponding to enabled entries in the selected MO2 modlist.txt will be imported."
                              Margin="0,0,0,5"/>

                    <Grid Margin="15,0,0,5"
                          Visibility="{Binding FilterByActiveModsMO2, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding MO2ModlistPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 IsReadOnly="True" Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectMO2ModlistCommand}"/>
                    </Grid>
                    <TextBlock Text="Select your MO2 profile's modlist.txt to filter which mod folders are imported."
                               FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="15,0,0,5"
                               Visibility="{Binding FilterByActiveModsMO2, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                </StackPanel>
            </Expander>

            <!-- Section 3: Output Settings -->
            <Expander Header="Output Settings" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionOutputSettings.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <!-- Output Directory (New) -->
                    <TextBlock Text="Output Directory:" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding OutputDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectOutputDirectoryCommand}"/>
                    </Grid>
                    <TextBlock Text="Directory where the generated plugin and related files will be saved." FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,5"/>

                    <!-- Append Date/Time Stamp (New) -->
                    <CheckBox Content="Append Date/Time Stamp to Output Directory"
                              IsChecked="{Binding AppendTimestampToOutputDirectory, Mode=TwoWay}"
                              Margin="0,0,0,10"
                              ToolTip="If checked, a timestamp (YYYYMMDD_HHMMSS) will be appended to the Output Directory name upon generation."/>

                    <!-- Output Plugin Name (Was Output Mod Name) -->
                    <TextBlock Text="Output Plugin Name:" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding TargetPluginName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>
                    <!-- Output Mod Name (Conceptual - No UI Element Needed, value comes from model default) -->

                    <!-- Patching Mode (New) -->
                    <TextBlock Text="Patching Mode:" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding PatchingModes}"
                                  SelectedItem="{Binding SelectedPatchingMode, Mode=TwoWay}"
                                  Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Content="?" Command="{Binding ShowPatchingModeHelpCommand}" Padding="6,2"/>
                    </Grid>

                    <TextBlock Text="Override Handling Mode:" Margin="0,5,0,2"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <ComboBox ItemsSource="{Binding RecordOverrideHandlingModes}"
                                  SelectedItem="{Binding SelectedRecordOverrideHandlingMode, Mode=TwoWay}"
                                  MinWidth="120"
                                  ToolTip="Determines how overriden records of other plugins are handled."/>
                        <Button Content="?" Command="{Binding ShowOverrideHandlingModeHelpCommand}" Padding="6,2" Margin="5,0,0,0"/>
                        
                        <!-- Override Handling Sub-options (inline) -->
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="15,0,0,0"
                                    Visibility="{Binding IsDefaultOverrideHandlingControlsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <TextBlock Text="Max Nested Search Layers:" VerticalAlignment="Center" Margin="0,0,5,0"
                                       Visibility="{Binding IsDefaultMaxNestedIntervalDepthVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <TextBox Text="{Binding DefaultMaxNestedIntervalDepth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                     Width="40" VerticalAlignment="Center"
                                     ToolTip="How many layers deep to keep searching for overridden records between finding relevant ones. This is not a total depth limit — the count resets every time a record from the mod is found, so a continuous chain of the mod's records is always followed all the way down. Default is 2, which is what lets the search cross one unrelated record (e.g. a vanilla outfit) to reach the mod's edits below it. Higher values find more obscure overrides but take exponentially longer."
                                     PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                     DataObject.Pasting="IntegerTextBox_Pasting"
                                     Visibility="{Binding IsDefaultMaxNestedIntervalDepthVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <CheckBox Content="Include All"
                                      IsChecked="{Binding DefaultIncludeAllOverrides, Mode=TwoWay}"
                                      VerticalAlignment="Center" Margin="15,0,0,0"
                                      ToolTip="If checked, grabs ALL override records from the plugins rather than searching by NPC dependencies. Use with caution."/>

                            <!-- Default for every mod that has not set its own. Hidden under
                                 "Include All", which does not traverse from the NPC at all. -->
                            <Button Content="{Binding DefaultOverrideTraversalRootsSummary}"
                                    Command="{Binding SetDefaultOverrideTraversalRootsCommand}"
                                    Margin="15,0,0,0" Padding="6,1"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding IsDefaultMaxNestedIntervalDepthVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="320">
                                            The default choice of which NPC-record fields the search for a mod's
                                            overridden records may start from. Individual mods can override this
                                            from their own "Override Roots" button.
                                            <LineBreak/><LineBreak/>
                                            A selected field is followed as deep as it goes, through any kind of
                                            record. This does not limit which record types can be merged in — only
                                            where the search begins.
                                        </TextBlock>
                                    </ToolTip>
                                </Button.ToolTip>
                            </Button>
                        </StackPanel>
                    </StackPanel>

                    <CheckBox Content="SkyPatcher Mode"
                              IsChecked="{Binding UseSkyPatcherMode, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="Generates a SkyPatcher .ini file for runtime patching instead of a traditional ESP. This is an advanced feature that may conflict with other runtime patchers."/>

                    <CheckBox Content="Auto-ESLify If Possible"
                              IsChecked="{Binding AutoEslIfy, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="When writing the output plugin, checks if it can be ESLified and performs the ESLification proccess"/>
                    <CheckBox Content="Auto-split if over the master limit"
                              IsChecked="{Binding AutoSplitOutput, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="If the output plugin would exceed Skyrim's 255-master limit, automatically split it into multiple files (e.g. &quot;MyPatch.esp&quot;, &quot;MyPatch_2.esp&quot;, ...). Only triggers on overflow - a normal-sized patch stays a single plugin. When it does split, ALL of the resulting plugins must be enabled in your load order. Also applies per-plugin when 'Split Output' is on."/>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0" VerticalAlignment="Center">
                        <CheckBox Content="Split Output" IsChecked="{Binding SplitOutput, Mode=TwoWay}" VerticalAlignment="Center"
                                  ToolTip="Splits the generated patch into multiple smaller plugins based on the criteria to the right."/>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding SplitOutput}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            
                            <TextBlock Text="by" VerticalAlignment="Center" Margin="0,0,10,0" FontStyle="Italic"/>
                            <CheckBox Content="Gender" IsChecked="{Binding SplitOutputByGender, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <CheckBox Content="Race" IsChecked="{Binding SplitOutputByRace, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox Text="{Binding SplitOutputMaxNpcs, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" Width="50" VerticalContentAlignment="Center"
                                     ToolTip="Maximum number of NPCs to place in a single split plugin."/>
                            <TextBlock Text="Max # NPCs" VerticalAlignment="Center" Margin="5,0,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Templated NPCs: whether a Traits-templated NPC keeps showing its
                         template's face or gets its own copy (Settings.TemplateHandlingMode).
                         Applies to record and SkyPatcher output alike. -->
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Templated NPCs:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox ItemsSource="{Binding TemplateHandlingModes}"
                                  SelectedValuePath="Key" DisplayMemberPath="Value"
                                  SelectedValue="{Binding SelectedTemplateHandlingMode, Mode=TwoWay}"
                                  MinWidth="220" VerticalAlignment="Center">
                            <ComboBox.ToolTip>
                                <ToolTip>
                                    <TextBlock TextWrapping="Wrap" Width="340">
                                        Some NPCs copy their face from another NPC instead of having one of their own
                                        (the NPC menu marks them with a Template icon).
                                        <LineBreak/><LineBreak/>
                                        Use the template's appearance (default): they keep showing the other NPC's face,
                                        and any appearance you pick for them is ignored.
                                        <LineBreak/><LineBreak/>
                                        Give each NPC its own copy: your choice is applied to each of them individually,
                                        so NPCs that share a template can look different. Adds one copy of the face files
                                        per NPC. NPCs whose face the game picks at random from a levelled list keep
                                        inheriting either way.
                                    </TextBlock>
                                </ToolTip>
                            </ComboBox.ToolTip>
                        </ComboBox>
                    </StackPanel>

                    <!-- Head Editing: wig/antler handling defaults + the manual antler
                         designation scope. Nested under Output Settings because these
                         only affect what the patcher writes. -->
                    <Expander Header="Head Editing" Style="{StaticResource SettingsSectionExpander}"
                              Margin="0,10,0,0"
                              IsExpanded="{Binding SectionHeadEditing.IsExpanded, Mode=TwoWay}">
                        <StackPanel>
                            <TextBlock Text="Default Wig Handling Mode:" Margin="0,0,0,2"/>
                            <ComboBox ItemsSource="{Binding WigHandlingModes}"
                                      SelectedValuePath="Key" DisplayMemberPath="Value"
                                      SelectedValue="{Binding SelectedDefaultWigHandlingMode, Mode=TwoWay}"
                                      MinWidth="120" HorizontalAlignment="Left"
                                      Margin="0,0,0,10">
                                <ComboBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            How detected wigs are handled during patching. Wigs come from two places: hair-slot
                                            wig armors in a mod's Default Outfits, and hair meshes carried directly in an NPC's
                                            skin (worn armor — the High Poly NPC Overhaul pattern). Applies in Create and Patch
                                            mode and in SkyPatcher mode; individual mods can override this in the Mods menu when
                                            a wig is detected. Antlers are controlled separately (below).
                                            <LineBreak/><LineBreak/>
                                            Forward To Skin: an outfit wig becomes part of the NPC's skin (WornArmor), so it
                                            shows no matter what outfit the NPC wears (falls back to Forward To Outfit if the
                                            appearance plugin assigns no WornArmor). A skin-carried wig is already in this
                                            state and is left as-is.
                                            <LineBreak/><LineBreak/>
                                            Forward To Outfit: the outfit the NPC actually wears in game is duplicated and
                                            the wig added to the duplicate (no change if it already contains the wig). A
                                            skin-carried wig is moved into that outfit as a generated wig armor.
                                            <LineBreak/><LineBreak/>
                                            Convert To Headparts (EXPERIMENTAL): the wig armor is discarded entirely and the
                                            wig becomes the NPC's hair — new head part records are generated and the wig mesh
                                            is baked into the FaceGen file (HDT-SMP physics is preserved). Survives outfit
                                            changes and helmets hide it like normal hair. Works for outfit wigs AND
                                            skin-carried wigs (the 3D preview's "Set Wig Meshes" button corrects which skin
                                            meshes count as wigs). An outfit wig falls back to Forward To Skin per NPC when
                                            the bake would be unsafe (e.g. the appearance mod ships no baked hair to
                                            replace); a skin-carried wig is left as-is instead.
                                            <LineBreak/><LineBreak/>
                                            Leave As Is: wigs are treated like any other outfit/skin element — an outfit wig is
                                            forwarded only if Include Outfits is selected; a skin-carried wig stays in the
                                            skin.
                                        </TextBlock>
                                    </ToolTip>
                                </ComboBox.ToolTip>
                            </ComboBox>

                            <TextBlock Text="Converted Wig Hair Color:" Margin="0,0,0,2"/>
                            <ComboBox ItemsSource="{Binding WigHairTintModes}"
                                      SelectedValuePath="Key" DisplayMemberPath="Value"
                                      SelectedValue="{Binding SelectedDefaultWigHairTintMode, Mode=TwoWay}"
                                      MinWidth="120" HorizontalAlignment="Left"
                                      Margin="0,0,0,10">
                                <ComboBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            Only applies to wigs handled with Convert To Headparts. Many wig meshes are
                                            authored with a near-black placeholder hair color because they expect RaceMenu
                                            to recolor them in game (RaceMenu automatically tints worn hair-slot items — it
                                            is why some mods, notably High Poly NPC Overhaul, give every NPC black hair
                                            until RaceMenu is installed). Once a wig is baked into the FaceGen file it is no
                                            longer a worn item, so RaceMenu stops recoloring it and the placeholder becomes
                                            permanent. This setting bakes the NPC's own hair color in instead, which is what
                                            the Creation Kit does for normal hair.
                                            <LineBreak/><LineBreak/>
                                            Auto: recolor only wigs that carry a placeholder tint. Wigs whose textures are
                                            already colored by their author (a neutral white tint) are left alone. The
                                            default, and the safe choice.
                                            <LineBreak/><LineBreak/>
                                            Always: recolor every converted wig with the NPC's hair color, exactly as
                                            RaceMenu would have done to the original worn wig.
                                            <LineBreak/><LineBreak/>
                                            Never: leave the wig's baked hair color untouched.
                                        </TextBlock>
                                    </ToolTip>
                                </ComboBox.ToolTip>
                            </ComboBox>

                            <CheckBox Content="Preview worn wigs the way RaceMenu tints them"
                                      IsChecked="{Binding EmulateRaceMenuHairSlotTint, Mode=TwoWay}"
                                      Margin="0,0,0,10">
                                <CheckBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            Affects mugshots and the 3D preview only — never the patch output. RaceMenu
                                            automatically recolors worn hair-slot items that use the Hair Tint shader,
                                            which the base game does not; wig meshes are usually authored with a
                                            near-black placeholder color that relies on it. With this on, wigs are
                                            previewed using the NPC's hair color, matching what you see in game with
                                            RaceMenu installed. Turn it off if you don't use RaceMenu — then the black
                                            hair shown here is what the game really renders.
                                        </TextBlock>
                                    </ToolTip>
                                </CheckBox.ToolTip>
                            </CheckBox>

                            <TextBlock Text="Manual Wig Designation Scope:" Margin="0,0,0,2"/>
                            <ComboBox ItemsSource="{Binding WigBlockScopes}"
                                      SelectedValuePath="Key" DisplayMemberPath="Value"
                                      SelectedValue="{Binding SelectedWigBlockScope, Mode=TwoWay}"
                                      MinWidth="120" HorizontalAlignment="Left"
                                      Margin="0,0,0,10">
                                <ComboBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            How broadly a skin-carried hair mesh you manually mark as a wig — or as NOT a
                                            wig — (via the 3D preview's "Set Wig Meshes" button) applies. This controls
                                            scope only — a wig is still converted only when its mod's Wig Handling Mode
                                            is Convert To Head Parts.
                                            <LineBreak/><LineBreak/>
                                            All NPCs (any mod): apply the designation to that mesh's EditorID wherever it
                                            appears (the default).
                                            <LineBreak/><LineBreak/>
                                            Same mod only: apply it only in the mod it was designated in.
                                            <LineBreak/><LineBreak/>
                                            Specific NPC(s) only: apply it only on the NPC(s) it was designated on.
                                        </TextBlock>
                                    </ToolTip>
                                </ComboBox.ToolTip>
                            </ComboBox>

                            <TextBlock Text="Default Antler Handling Mode:" Margin="0,0,0,2"/>
                            <ComboBox ItemsSource="{Binding AntlerHandlingModes}"
                                      SelectedValuePath="Key" DisplayMemberPath="Value"
                                      SelectedValue="{Binding SelectedDefaultAntlerHandlingMode, Mode=TwoWay}"
                                      MinWidth="120" HorizontalAlignment="Left"
                                      Margin="0,0,0,10">
                                <ComboBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            How detected antler armors/head parts are handled during patching. Applies in
                                            Create and Patch mode and in SkyPatcher mode; individual mods can override this
                                            in the Mods menu when an antler is detected.
                                            <LineBreak/><LineBreak/>
                                            Forward To Skin: the antler becomes part of the NPC's skin (WornArmor), so it shows
                                            no matter what outfit the NPC wears. Falls back to Forward To Outfit if the
                                            appearance plugin assigns no WornArmor. (The pre-split default — leave this to keep
                                            existing behavior.)
                                            <LineBreak/><LineBreak/>
                                            Forward To Outfit: the outfit the NPC actually wears in game is duplicated and
                                            the antler added to the duplicate.
                                            <LineBreak/><LineBreak/>
                                            Remove: keep the antler OFF the NPC entirely — stripped from a forwarded outfit,
                                            from the worn armor, and from the FaceGen (keyword-detected antler head parts).
                                            Antlers baked into an outfit the load order owns (not forwarded) can't be reached.
                                            <LineBreak/><LineBreak/>
                                            Leave As Is: the antler is treated like any other outfit element — forwarded only if
                                            Include Outfits is selected.
                                        </TextBlock>
                                    </ToolTip>
                                </ComboBox.ToolTip>
                            </ComboBox>

                            <TextBlock Text="Manual Antler Block Scope:" Margin="0,0,0,2"/>
                            <ComboBox ItemsSource="{Binding AntlerBlockScopes}"
                                      SelectedValuePath="Key" DisplayMemberPath="Value"
                                      SelectedValue="{Binding SelectedAntlerBlockScope, Mode=TwoWay}"
                                      MinWidth="120" HorizontalAlignment="Left">
                                <ComboBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            How broadly a head part you manually designate as an antler (via the 3D preview's
                                            "Set Antler Head Parts" button) is blocked. This controls scope only — a head part
                                            is still removed only when its mod's Antler Handling Mode is Remove.
                                            <LineBreak/><LineBreak/>
                                            All NPCs (any mod): block that head part's EditorID wherever it appears (the default).
                                            <LineBreak/><LineBreak/>
                                            Same mod only: block it only in the mod it was designated in.
                                            <LineBreak/><LineBreak/>
                                            Specific NPC(s) only: block it only on the NPC(s) it was designated on, regardless
                                            of which mod supplies that NPC's appearance.
                                        </TextBlock>
                                    </ToolTip>
                                </ComboBox.ToolTip>
                            </ComboBox>

                            <CheckBox Content="Publish forwarded outfits to SkyPatcher/SPID"
                                      IsChecked="{Binding PublishForwardedOutfitsToDistributors, Mode=TwoWay}"
                                      Margin="0,10,0,0">
                                <CheckBox.ToolTip>
                                    <ToolTip>
                                        <TextBlock TextWrapping="Wrap" Width="340">
                                            Some NPCs have their outfit assigned at runtime by SkyPatcher or SPID rather
                                            than by a plugin record. For those NPCs a wig or antler forwarded to the
                                            outfit would be wiped out in game, because the distributor overwrites the
                                            outfit the patch assigned.
                                            <LineBreak/><LineBreak/>
                                            With this on, the patcher writes its own config for exactly those NPCs — a
                                            SkyPatcher entry when a SkyPatcher config contests the outfit, a generated
                                            *_DISTR.ini when a SPID config does — pointing at the duplicated outfit that
                                            carries the wig/antler, so it survives. Nothing is written for NPCs whose
                                            outfit no distributor touches.
                                            <LineBreak/><LineBreak/>
                                            With this off, those NPCs are only warned about in the patch log and the
                                            forwarded wig/antler will most likely not appear in game.
                                        </TextBlock>
                                    </ToolTip>
                                </CheckBox.ToolTip>
                            </CheckBox>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </Expander>

            <Expander Header="Mugshot Settings" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionMugshotSettings.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <TextBlock Text="Mugshots Folder:" Margin="0,0,0,2"/>
                    <Grid Margin="0,0,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding MugshotsFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                        <Button Grid.Column="1" Content="Browse..." Command="{Binding SelectMugshotsFolderCommand}"/>
                    </Grid>
                    <TextBlock Text="Folder containing subfolders for each mod's NPC preview images."
                               FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,10"/>

                    <!-- Mugshot Source Priority: drag-to-reorder. Disabled sources stay in the
                         list but are greyed out via BooleanToOpacityConverter and skipped at
                         runtime. Persistence is wired in VM_Settings via CollectionChanged. -->
                    <TextBlock Text="Mugshot Source Priority:" Margin="0,0,0,2"/>
                    <TextBlock Text="Drag to reorder. Disabled sources are skipped at runtime."
                               FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,4"/>
                    <ListBox ItemsSource="{Binding MugshotSourcePriority}"
                             dd:DragDrop.IsDragSource="True"
                             dd:DragDrop.IsDropTarget="True"
                             BorderBrush="{DynamicResource BorderBrush}"
                             BorderThickness="1"
                             Margin="0,0,0,10"
                             HorizontalAlignment="Left"
                             MinWidth="220">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid ToolTip="{Binding DisabledReason}"
                                      Opacity="{Binding IsEnabled, Converter={StaticResource BooleanToOpacityConverter}}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="≡  "
                                               Foreground="{DynamicResource HintForeground}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding DisplayName}"/>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <CheckBox Content="Enable Live Tiles on Selection"
                              IsChecked="{Binding EnableLiveTilesOnSelection, Mode=TwoWay}"
                              Margin="0,0,0,2"
                              ToolTip="Allows the 'Live Tile' mugshot source above: when NPC selection reaches it, the tile becomes an embedded 3D viewport rendered on the spot instead of a static image. Costs a render per tile each time an NPC is selected. Individual tiles can always be switched via right-click &gt; Make Live Tile, regardless of this setting."/>
                    <TextBlock Text="Un-greys the 'Live Tile' source above. Place it first to always get live 3D tiles, or last as a fallback when no image exists."
                               FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,10"/>

                    <CheckBox Content="Use FaceFinder API for missing mugshots"
                              IsChecked="{Binding UseFaceFinderFallback, Mode=TwoWay}"
                              Margin="0,0,0,5"/>
                    
                    <Border Margin="15,5,0,5" BorderBrush="Gray" BorderThickness="1" CornerRadius="3"
                            Visibility="{Binding UseFaceFinderFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Margin="5">
                            <TextBlock Text="FaceFinder Cache Folder:" Margin="0,5,0,2"/>
                            <Grid Margin="0,0,0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding FaceFinderMugshotsFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,5,0"
                                         ToolTip="Folder where FaceFinder downloads are cached. Defaults to a 'FaceFinder Cache' folder next to the application."/>
                                <Button Grid.Column="1" Content="Browse..."
                                        Command="{Binding SelectFaceFinderMugshotsFolderCommand}"/>
                                <Button Grid.Column="2" Content="Reset" Margin="5,0,0,0"
                                        Command="{Binding ResetFaceFinderMugshotsFolderCommand}"
                                        ToolTip="Reset to the default FaceFinder cache folder."/>
                            </Grid>
                            <TextBlock Text="FaceFinder downloads are cached here, in subfolders per mod."
                                       FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,5"/>

                            <CheckBox Content="Cache downloaded images locally"
                                      IsChecked="{Binding CacheFaceFinderImages, Mode=TwoWay}"
                                      Margin="0,10,0,0"
                                      ToolTip="If checked, images downloaded from FaceFinder are saved and checked for updates. If unchecked, images are downloaded fresh every time."
                                      Visibility="{Binding UseFaceFinderFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            
                            <CheckBox Content="Log API Requests/Responses"
                                      IsChecked="{Binding LogFaceFinderRequests, Mode=TwoWay}"
                                      Margin="0,5,0,0"
                                      ToolTip="Writes API traffic to FaceFinderLog.html in the application directory (open it in any web browser). Each request is a collapsible entry with its status, headers, and full response body."
                                      Visibility="{Binding UseFaceFinderFallback, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0"
                                        HorizontalAlignment="Left"
                                        Visibility="{Binding UseFaceFinderFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <Button Content="Link Mod Names to FaceFinder"
                                        Command="{Binding OpenModLinkerCommand}"
                                        Padding="10,5"
                                        ToolTip="Manually link local mod names to their corresponding entry on the FaceFinder server."/>
                                <Button Content="Batch Download Mugshots"
                                        Command="{Binding BatchDownloadFaceFinderMugshotsCommand}"
                                        Padding="10,5"
                                        Margin="15,0,0,0"
                                        ToolTip="Iterates every mod's NPCs and queries the FaceFinder API for an available mugshot. When image caching is on, hits are downloaded and persisted; when off, only availability is checked. Independent of the local-renderer 'Generate All Mugshots' batch — neither falls back to the other."/>
                            </StackPanel>

                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Button Grid.Column="0" Content="Delete All Cached FaceFinder Images"
                                        Command="{Binding DeleteCachedFaceFinderImagesCommand}"
                                        Padding="10,5"
                                        ToolTip="Permanently deletes all cached FaceFinder images and their metadata files."
                                        Visibility="{Binding UseFaceFinderFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                            <Setter Property="Background" Value="#FFE57373"/>
                                            <Setter Property="Foreground" Value="White"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FFD32F2F"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="Search Mode:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                    <ComboBox ItemsSource="{Binding MugshotSearchModes}"
                                              SelectedItem="{Binding SelectedMugshotSearchModeFF, Mode=TwoWay}"
                                              MinWidth="120"
                                              ToolTip="Fast: Uses cached list of generated mugshots (recommended). Comprehensive: Scans all mugshot folders and reads metadata."/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                    
                    <CheckBox Content="Auto-Generate missing mugshots"
                              IsChecked="{Binding UsePortraitCreatorFallback, Mode=TwoWay}"
                              Margin="0,5,0,0"/>

                    <!-- Renderer selection: Internal (in-process) vs Legacy NPC Portrait Creator subprocess -->
                    <StackPanel Orientation="Horizontal" Margin="15,5,0,5"
                                Visibility="{Binding UsePortraitCreatorFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Renderer:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox ItemsSource="{Binding RendererChoices}"
                                  SelectedItem="{Binding SelectedRenderer, Mode=TwoWay}"
                                  Width="260" />
                    </StackPanel>

                    <!-- Auto-Generated Mugshots folder selector -->
                    <StackPanel Margin="15,5,0,5"
                                Visibility="{Binding UsePortraitCreatorFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Auto-Generated Mugshots Folder:" Margin="0,0,0,2"/>
                        <Grid Margin="0,0,0,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Grid.Column="0"
                                     Text="{Binding AutogenMugshotsFolder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Margin="0,0,5,0"
                                     ToolTip="Folder where rendered mugshots are saved. Defaults to an 'AutoGen Mugshots' folder next to the application."/>
                            <Button Grid.Column="1" Content="Browse..."
                                    Command="{Binding SelectAutogenMugshotsFolderCommand}"/>
                            <Button Grid.Column="2" Content="Reset" Margin="5,0,0,0"
                                    Command="{Binding ResetAutogenMugshotsFolderCommand}"
                                    ToolTip="Reset to the default auto-generated mugshots folder."/>
                        </Grid>
                        <TextBlock Text="Internal/Legacy renderer output is saved here, in subfolders per mod."
                                   FontSize="10" Foreground="{DynamicResource HintForeground}" Margin="0,0,0,5"/>
                    </StackPanel>

                    <!-- Batch mugshot generation: scope dropdown ("All" or a single
                         mod) + the generate button + the asset-validated toggle. -->
                    <StackPanel Orientation="Horizontal" Margin="15,5,0,5"
                                Visibility="{Binding UsePortraitCreatorFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Button Content="Batch Generate Mugshots"
                                Command="{Binding GenerateAllMugshotsCommand}"
                                Padding="10,5"
                                Margin="0,0,8,0"
                                ToolTip="Generates a mugshot for each local NPC that doesn't already have one, using the renderer selected above and the scope chosen in the dropdown to the right. NPCs are grouped by sex and shared NPC-record assets (race, worn armor, head parts, hair) so the renderer reuses meshes and textures across consecutive frames. FaceFinder downloads are handled separately via the FaceFinder section's 'Batch Download Mugshots' button."/>
                        <ComboBox x:Name="MugshotGenerationModComboBox"
                                  ItemsSource="{Binding MugshotGenerationModOptions}"
                                  SelectedItem="{Binding SelectedMugshotGenerationMod, Mode=TwoWay}"
                                  DropDownOpened="MugshotGenerationModComboBox_DropDownOpened"
                                  MinWidth="140" MaxWidth="260"
                                  VerticalAlignment="Center"
                                  Margin="0,0,8,0"
                                  ToolTip="Choose which mod to generate mugshots for. 'All' processes every data-containing (non-mugshot-only) mod; selecting a specific mod limits the batch to that mod's NPCs."/>
                        <CheckBox Content="Asset-Validated Only"
                                  IsChecked="{Binding AssetValidatedMugshotsOnly, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  Margin="10,0,0,0"
                                  ToolTip="When checked, an Internal-renderer mugshot whose render reports any missing meshes or textures is skipped — no PNG is written — so the batch only persists complete renders. Uncheck to save the wireframe / placeholder-textured result anyway. Per-tile clicks ignore this toggle and always render."/>
                    </StackPanel>

                    <!-- Internal renderer settings panel -->
                    <Border Margin="15,5,0,5" Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="3">
                        <Border.Resources>
                            <!-- Theme-aware default for every xctk:DoubleUpDown
                                 inside this panel. The toolkit's default template
                                 inherits the system-light Windows theme (white
                                 textbox interior) which renders as invisible /
                                 fighting-the-theme against the host's dark
                                 background. Setting Background + Foreground +
                                 BorderBrush + CaretBrush at the spinner root
                                 propagates through the embedded TextBox and
                                 increment/decrement glyphs. -->
                            <Style TargetType="{x:Type xctk:DoubleUpDown}">
                                <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="CaretBrush" Value="{DynamicResource PrimaryForeground}"/>
                            </Style>
                        </Border.Resources>
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding UsePortraitCreatorFallback}" Value="True"/>
                                            <Condition Binding="{Binding IsInternalRenderer}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <!-- Header row: title + Show/Hide Preview toggle + Reset
                                 Settings. Plain Button (rather than ToggleButton) for the
                                 toggle so the system "checked-state" styling - which paints
                                 a light-blue highlight that fights the host's dark theme
                                 and crushes contrast on the white label - never kicks in.
                                 State is conveyed via the Content swap below. Reset Settings
                                 lives here (rather than in the preview UC's toolbar) so it
                                 stays reachable when the preview is hidden. -->
                            <DockPanel LastChildFill="False" Margin="0,0,0,5">
                                <TextBlock Text="Internal Renderer (in-process CharacterViewer)"
                                           FontWeight="Bold" VerticalAlignment="Center"
                                           DockPanel.Dock="Left"/>
                                <!-- "Log Render Logic" docks first so it ends up at
                                     the rightmost edge (DockPanel right-docks in
                                     declaration order: first child = rightmost). -->
                                <CheckBox Content="Log Render Logic"
                                          IsChecked="{Binding InternalLogRenderLogic, Mode=TwoWay}"
                                          DockPanel.Dock="Right"
                                          VerticalAlignment="Center" Margin="6,0,0,0"
                                          ToolTip="When checked, every Internal-renderer load writes its full asset-resolution + renderer trace to &lt;ExeDir&gt;\RenderLogs\: live previews go to Preview_&lt;ModName&gt;_&lt;FormKey&gt;.txt, mugshot tile generations go to Mugshot_&lt;ModName&gt;_&lt;FormKey&gt;.txt. Use to diff a working preview against a tile that fails to render. Forces verbose logging on for the duration of the captured render."/>
                                <Button Content="Reset Settings"
                                        Command="{Binding InternalMugshotPreviewVM.ResetCommand}"
                                        Padding="10,3" DockPanel.Dock="Right" Margin="6,0,0,0"
                                        ToolTip="Restore Internal-renderer settings to defaults. Preserves your saved lighting layouts / color schemes."/>
                                <Button Command="{Binding ToggleInternalShowPreviewCommand}"
                                        Padding="10,3" DockPanel.Dock="Right"
                                        ToolTip="Toggle the live preview viewport's visibility. Hide it to claw back vertical space when you're tweaking framing / asset-resolution settings without needing to see the rendered output.">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                            <Setter Property="Content" Value="Show Preview"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding InternalShowPreview}" Value="True">
                                                    <Setter Property="Content" Value="Hide Preview"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DockPanel>

                            <!-- Camera mode + auto-mode tunables on a single horizontal line that wraps. -->
                            <WrapPanel Orientation="Horizontal" ItemHeight="28">
                                <StackPanel Orientation="Horizontal" Margin="0,0,12,0">
                                    <TextBlock Text="Camera Mode:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <ComboBox ItemsSource="{Binding InternalCameraModeChoices}"
                                              SelectedItem="{Binding InternalCameraMode, Mode=TwoWay}"
                                              Width="100" VerticalAlignment="Center"
                                              ToolTip="Auto: hair-aware framing (head + hair-above-head). Manual: use the saved orbit camera state from the live preview."/>
                                </StackPanel>

                                <!-- Auto-mode tunables - same row, hidden when Manual is selected. -->
                                <StackPanel Orientation="Horizontal">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding InternalCameraMode}" Value="Auto">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>

                                    <TextBlock Text="Frame Top:" VerticalAlignment="Center" Margin="0,0,4,0"
                                               ToolTip="Vertical position the TOP of the head bounding box is mapped to, expressed as a fraction of the rendered frame height (1.0 = frame top edge, 0.0 = frame bottom edge). Examples: 0.95 leaves a 5% margin above the head top; 1.0 puts the head top exactly at the frame's top edge; values above 1.0 push the head top above the frame for an ultra-tight crop where the top of the head is clipped."/>
                                    <xctk:DoubleUpDown Width="70" Minimum="-0.5" Maximum="1.5" Increment="0.05"
                                                       Value="{Binding InternalHeadTopFraction, Mode=TwoWay}"
                                                       VerticalAlignment="Center"
                                                       FormatString="0.00"
                                                       ToolTip="Vertical position the TOP of the head bounding box is mapped to, expressed as a fraction of the rendered frame height (1.0 = frame top edge, 0.0 = frame bottom edge). Examples: 0.95 leaves a 5% margin above the head top; 1.0 puts the head top exactly at the frame's top edge; values above 1.0 push the head top above the frame for an ultra-tight crop where the top of the head is clipped."/>

                                    <TextBlock Text="Bottom:" VerticalAlignment="Center" Margin="10,0,4,0"
                                               ToolTip="Vertical position the BOTTOM of the head bounding box is mapped to, expressed as a fraction of the rendered frame height (1.0 = frame top edge, 0.0 = frame bottom edge). Examples: 0.10 leaves a 10% margin below the head bottom; 0.0 puts the head bottom exactly at the frame's bottom edge; negative values (e.g. -0.1) push the head bottom below the frame for an ultra-tight crop where the chin / neck is clipped. Values above the Frame Top setting are nonsensical (head bottom above head top)."/>
                                    <xctk:DoubleUpDown Width="70" Minimum="-0.5" Maximum="1.5" Increment="0.05"
                                                       Value="{Binding InternalHeadBottomFraction, Mode=TwoWay}"
                                                       VerticalAlignment="Center"
                                                       FormatString="0.00"
                                                       ToolTip="Vertical position the BOTTOM of the head bounding box is mapped to, expressed as a fraction of the rendered frame height (1.0 = frame top edge, 0.0 = frame bottom edge). Examples: 0.10 leaves a 10% margin below the head bottom; 0.0 puts the head bottom exactly at the frame's bottom edge; negative values (e.g. -0.1) push the head bottom below the frame for an ultra-tight crop where the chin / neck is clipped."/>

                                    <TextBlock Text="Yaw:" VerticalAlignment="Center" Margin="10,0,4,0"
                                               ToolTip="Camera yaw in degrees. 180 = facing camera."/>
                                    <TextBox Text="{Binding InternalYaw, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="55" VerticalAlignment="Center"
                                             PreviewTextInput="FloatTextBox_PreviewTextInput"
                                             DataObject.Pasting="FloatTextBox_Pasting"/>

                                    <TextBlock Text="Pitch:" VerticalAlignment="Center" Margin="10,0,4,0"/>
                                    <TextBox Text="{Binding InternalPitch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="55" VerticalAlignment="Center"
                                             PreviewTextInput="FloatTextBox_PreviewTextInput"
                                             DataObject.Pasting="FloatTextBox_Pasting"/>

                                    <TextBlock Text="Hair pad:" VerticalAlignment="Center" Margin="10,0,4,0"/>
                                    <TextBox Text="{Binding InternalHairAbovePadding, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="55" VerticalAlignment="Center"
                                             PreviewTextInput="FloatTextBox_PreviewTextInput"
                                             DataObject.Pasting="FloatTextBox_Pasting"
                                             ToolTip="Hair-above-head padding (world units)."/>
                                </StackPanel>
                            </WrapPanel>

                            <!-- Decode-cache budget: how much RAM the in-process renderer's decoded-texture
                                 and parsed-mesh caches may use during mugshot generation. -->
                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                <TextBlock Text="Texture/geometry cache:" VerticalAlignment="Center" Margin="0,0,4,0"
                                           ToolTip="How the in-process renderer sizes its decoded-texture and parsed-mesh RAM caches during mugshot generation. '% Free RAM' (default) grows the cache into a share of spare system memory; 'Fixed RAM' caps the total at the budget below; 'Disabled' turns caching off (renders decode fresh each time — slower, minimal RAM). Takes effect within a few renders; no restart needed."/>
                                <ComboBox ItemsSource="{Binding MugshotCacheModeChoices}"
                                          SelectedItem="{Binding MugshotCacheMode, Mode=TwoWay}"
                                          Width="130" VerticalAlignment="Center">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <!-- Free-RAM percentage field: shown only when % Free RAM is selected. -->
                                <StackPanel Orientation="Horizontal" Margin="12,0,0,0"
                                            ToolTip="Total share of free RAM the decode caches may use together (they keep their fixed 75:9:1 ratio). 85 is the default; higher caches more aggressively, lower frees RAM for other work. Takes effect within a few renders; no restart needed.">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding MugshotCacheMode}" Value="PercentFreeRam">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="Free RAM %:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <xctk:DoubleUpDown Width="75" Minimum="0" Maximum="100" Increment="5"
                                                       Value="{Binding MugshotCacheFreeRamPercent, Mode=TwoWay}"
                                                       FormatString="0.##" VerticalAlignment="Center"
                                                       ToolTip="Total share of free RAM (0-100) the renderer's decode caches may collectively use in % Free RAM mode."/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                                    <StackPanel.Style>
                                        <Style TargetType="StackPanel">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding MugshotCacheMode}" Value="FixedRam">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </StackPanel.Style>
                                    <TextBlock Text="Budget (GB):" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <xctk:DoubleUpDown Width="75" Minimum="0.25" Maximum="256" Increment="0.5"
                                                       Value="{Binding MugshotCacheFixedGB, Mode=TwoWay}"
                                                       FormatString="0.##" VerticalAlignment="Center"
                                                       ToolTip="Approximate total RAM (GB) the renderer's decode caches may use in Fixed RAM mode."/>
                                </StackPanel>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding InternalCameraMode}" Value="Auto">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <CheckBox Content="Include hair / brows / mouth in framing"
                                          IsChecked="{Binding InternalIncludeAccessories, Mode=TwoWay}"
                                          ToolTip="When enabled, hair-above-head and other facial accessories help frame the shot - but floor-length braids are clipped at the head's lower bound so they don't blow out the frame."/>
                            </StackPanel>

                            <!-- Background RGB + Output resolution share a single wrap row. -->
                            <WrapPanel Orientation="Horizontal" Margin="0,8,0,0" ItemHeight="28">
                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                    <TextBlock Text="Background:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <Border BorderBrush="Black" BorderThickness="1" Width="60" Height="22"
                                            Background="{Binding InternalBackgroundColor}" CornerRadius="2"
                                            VerticalAlignment="Center"/>
                                    <Button Content="Change..." Margin="6,0,0,0" Padding="6,1"
                                            VerticalAlignment="Center"
                                            Command="{Binding SelectInternalBackgroundColorCommand}"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Output:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <TextBox Text="{Binding InternalOutputWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="55" VerticalAlignment="Center"
                                             PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                             DataObject.Pasting="IntegerTextBox_Pasting"/>
                                    <TextBlock Text=" x " VerticalAlignment="Center" Margin="3,0"/>
                                    <TextBox Text="{Binding InternalOutputHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             Width="55" VerticalAlignment="Center"
                                             PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                             DataObject.Pasting="IntegerTextBox_Pasting"/>
                                </StackPanel>
                            </WrapPanel>

                            <!-- Asset-resolution toggles - feed into OffscreenRenderRequest /
                                 VM_CharacterViewer for the next render so changes take effect
                                 immediately. Render-pipeline toggles (tone-mapping, shadows,
                                 SSAO, eye catch-light, SSS strength, vignette, missing-tex
                                 wireframe) live in the renderer's UC_CharacterViewerRenderPanel
                                 inside the live-preview toolbar - shared with SynthEBD. -->
                            <WrapPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Asset Resolution:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <CheckBox Content="BSA &lt; loose"
                                          IsChecked="{Binding InternalVanillaLooseOverridesBsa, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on (default), a loose copy of an asset under your Skyrim Data folder takes precedence over any BSA copy of the same path - including BSAs scoped to a non-vanilla mod. Mirrors the engine's actual rule that loose Data files override BSA-packed ones. Turn off for strict-BSA mode (preview the original mod content without your installed loose-file overrides)."/>
                                <CheckBox Content="Mod loose &lt; Data loose"
                                          IsChecked="{Binding InternalVanillaLooseOverridesModLoose, Mode=TwoWay}"
                                          VerticalAlignment="Center"
                                          ToolTip="When on, a loose copy of an asset in your Skyrim Data folder takes precedence over the same path in any mod folder, letting your installed body / skin / texture replacers leak into mod-specific previews. FaceGen NIFs and FaceTint DDS are excluded so the mod's actual face overrides aren't replaced by vanilla copies. Off by default."/>
                            </WrapPanel>

                            <!-- Re-render triggers: each toggle gates one staleness check -
                                 renderer/program-version drift, settings/condition drift, and
                                 recorded missing-asset drift (split into base NPC assets vs
                                 outfit/headgear assets). All default on; turn one off to stop
                                 that class of automatic regeneration. Backing gates live in
                                 MugshotStalenessChecker (Internal) / PortraitCreator (Legacy). -->
                            <WrapPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Re-render When:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <CheckBox Content="Newer renderer version"
                                          IsChecked="{Binding AutoUpdateOldMugshots, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on (default), a mugshot produced by an older version of the renderer (Internal) or the NPC Portrait Creator (Legacy) is treated as stale and re-rendered, so it picks up rendering fixes and improvements from the current version. Turn off to keep older-version mugshots in place."/>
                                <CheckBox Content="Stale Conditions"
                                          IsChecked="{Binding AutoUpdateStaleMugshots, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on (default), a mugshot is re-rendered when the settings it was generated with - lighting, camera / framing, background, resolution and render options - no longer match your current settings. Turn off to keep existing mugshots in place even after you change these settings."/>
                                <CheckBox Content="Missing NPC Assets"
                                          IsChecked="{Binding AutoUpdateMugshotsWithMissingAssets, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on (default), a previously-generated mugshot whose stamped metadata records missing BASE NPC assets — the NPC's own head/body/hair meshes or textures, or a FaceGen mismatch — is treated as stale and re-rendered on the next session, giving newly-installed assets a chance to fill in the wireframe / placeholder areas automatically. Turn off to keep the existing PNG in place even when it depicts an incomplete model."/>
                                <CheckBox Content="Missing Outfit Assets"
                                          IsChecked="{Binding AutoUpdateMugshotsWithMissingOutfitAssets, Mode=TwoWay}"
                                          VerticalAlignment="Center"
                                          ToolTip="When on (default), a mugshot whose stamped metadata records missing outfit/headgear meshes or textures is re-rendered on the next session so newly-installed attire assets fill in the wireframe / placeholder areas. (Broken outfit physics links are excluded: nothing you install can fix a stale link inside the mod.) Turn off to keep the existing PNG in place."/>
                            </WrapPanel>

                            <!-- Warning-icon visibility: each gates one mugshot-tile warning
                                 icon AND whether its backing state is recorded in the PNG
                                 metadata. Unchecking re-renders exactly the mugshots that
                                 were displaying that icon so they come back clean. -->
                            <WrapPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Warning Icons:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <CheckBox Content="Show Missing NPC Assets Icon"
                                          IsChecked="{Binding InternalShowMissingNpcAssetsIcon, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on (default), a mugshot rendered with missing meshes/textures (or a detected FaceGen mismatch) shows the missing-asset warning icon, and that state is recorded in the PNG so the icon survives restarts. Unchecking hides the icon and re-renders the affected mugshots once so their recorded state is cleared. Note: while off, newly-rendered mugshots record no missing assets, so the 'Re-render When: Missing Assets' option has nothing to act on for them."/>
                                <CheckBox Content="Show Missing Outfit Assets Icon"
                                          IsChecked="{Binding InternalShowMissingOutfitAssetsIcon, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on (default), a mugshot whose outfit/headgear is missing meshes or textures — or references a physics config that doesn't exist (a broken SMP/HDT link inside the mod; the render itself is correct) — shows the outfit-assets warning icon, recorded in the PNG so it survives restarts. The missing meshes/textures re-render when you install the assets (via 'Re-render When: Missing Assets'); the broken physics link is informational. Unchecking hides the icon and re-renders the affected mugshots once so their recorded state is cleared."/>
                                <CheckBox Content="Show Data Folder Assets Icon"
                                          IsChecked="{Binding InternalShowDataFolderAssetsIcon, Mode=TwoWay}"
                                          VerticalAlignment="Center"
                                          ToolTip="When on (default), a mugshot that pulled non-vanilla assets from your data folder — because they were not found in the mod's Corresponding Mod Folders — shows the data-folder-assets icon, recorded in the PNG so it survives restarts. These assets come from some other installed mod: it must stay activated, or be added to this mod's Corresponding Mod Folders, for the NPC to look right in game. Informational only — the render itself is correct. Unchecking hides the icon and re-renders the affected mugshots once so their recorded state is cleared."/>
                            </WrapPanel>

                            <!-- Attire defaults: apply to both the mugshot renderer and
                                 the 3D preview. The full-screen 3D preview exposes the
                                 same two toggles as non-persistent per-view overrides. -->
                            <WrapPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="Attire:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <CheckBox Content="Include default outfit"
                                          IsChecked="{Binding InternalIncludeDefaultOutfit, Mode=TwoWay}"
                                          VerticalAlignment="Center" Margin="0,0,12,0"
                                          ToolTip="When on, render the NPC's default outfit in mugshots and the 3D preview. Body-covering armor hides the nude body underneath. On by default."/>
                                <CheckBox Content="Include headgear"
                                          IsChecked="{Binding InternalIncludeHeadgear, Mode=TwoWay}"
                                          Visibility="{Binding InternalIncludeDefaultOutfit, Converter={StaticResource BooleanToVisibilityConverter}}"
                                          VerticalAlignment="Center"
                                          ToolTip="When on, render worn/outfit head gear (helmets/hoods/circlets); hair is hidden under headgear, as in game. Off by default — the sensible choice for a face-picking tool."/>
                            </WrapPanel>

                            <!-- Live preview - wrapped in a Grid so its Visibility binding
                                 reads InternalShowPreview off the inherited VM_Settings
                                 DataContext rather than the UC's overridden one. -->
                            <Grid Margin="0,12,0,0"
                                  Visibility="{Binding InternalShowPreview, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <local:UC_InternalMugshotPreview DataContext="{Binding InternalMugshotPreviewVM}"
                                                                  ShowAttireToggles="False"
                                                                  Height="540"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Legacy NPC Portrait Creator settings panel -->
                    <Border Margin="15,5,0,5" Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="3">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding UsePortraitCreatorFallback}" Value="True"/>
                                            <Condition Binding="{Binding IsLegacyRenderer}" Value="True"/>
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock Text="Max Parallel Portrait Renders" FontWeight="Bold" Margin="0,5,0,0"/>
                            <TextBlock TextWrapping="Wrap" Margin="0,0,0,5" Opacity="0.8">
                                Controls how many CPU-intensive portrait generation processes can run at once.
                                Lower values reduce system load during image generation.
                            </TextBlock>
                            <TextBox Text="{Binding MaxParallelPortraitRenders}" 
                                     Width="60" HorizontalAlignment="Left"
                                     PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                     DataObject.Pasting="IntegerTextBox_Pasting" />

                            <TextBlock Text="Mugshot Background Color" FontWeight="Bold" Margin="0,15,0,0"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,5">
                                <Border BorderBrush="Black" BorderThickness="1" Width="100" Height="25"
                                        Background="{Binding MugshotBackgroundColor}" CornerRadius="2"/>
                                <Button Content="Change..." Margin="10,0,0,0"
                                        Command="{Binding SelectBackgroundColorCommand}" VerticalAlignment="Center"/>
                            </StackPanel>
                            
                            <CheckBox Content="Enable Normal Map Hack"
                                      IsChecked="{Binding EnableNormalMapHack, Mode=TwoWay}"
                                      Margin="0,10,0,0">
                                <CheckBox.ToolTip>
                                    <TextBlock TextWrapping="Wrap" MaxWidth="400">
                                        The normal maps as loaded by the Nif model don't produce maximal brightness when illuminated directly, but rather at an angle. This seems physically incorrect, since things should generally look brightest when illuminated head-on. This hack explicitly rotates non-model-space normals to maximally illuminate in the direction of the incident light, making things like eyes and hair look brighter. I think this is more accurate than default, but you can disable this if you think the generated portraits have too much glare.
                                    </TextBlock>
                                </CheckBox.ToolTip>
                            </CheckBox>
                            
                            <CheckBox Content="Use Modded Fallback Textures"
                                      IsChecked="{Binding UseModdedFallbackTextures, Mode=TwoWay}"
                                      Margin="0,10,0,0">
                                <CheckBox.ToolTip>
                                    <TextBlock TextWrapping="Wrap" MaxWidth="400">
                                        If enabled (default), missing textures will search ALL files (loose or BSA) in the base game Data folder as a last resort. For example, vanilla skinned NPCs will use your installed skin textures.
                                        If disabled, this fallback search is restricted to ONLY vanilla Skyrim and Creation Club BSAs, preventing loose or mod-added BSA textures in the base Data folder from overwriting the base game.
                                    </TextBlock>
                                </CheckBox.ToolTip>
                            </CheckBox>
                            
                            <TextBlock Text="Lighting Profile (JSON)" FontWeight="Bold" Margin="0,10,0,0"/>
                            <TextBox Text="{Binding DefaultLightingJsonString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     AcceptsReturn="True" TextWrapping="Wrap" MaxHeight="200"
                                     VerticalScrollBarVisibility="Auto" FontFamily="Consolas">
                                <TextBox.Style>
                                    <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLightingJsonValid}" Value="True">
                                                <Setter Property="BorderBrush" Value="{DynamicResource StatusValidForeground}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsLightingJsonValid}" Value="False">
                                                <Setter Property="BorderBrush" Value="{DynamicResource StatusErrorForeground}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                            
                            <TextBlock Text="Camera Position:" Margin="0,15,0,2"/>
                            <ComboBox ItemsSource="{Binding CameraModes}" SelectedItem="{Binding SelectedCameraMode, Mode=TwoWay}" Margin="0,0,0,10"/>

                            <!-- NEW: FOV control (always visible) -->
            <TextBlock Text="Vertical Field of View (degrees):" Margin="0,0,0,2"/>
            <TextBox Text="{Binding VerticalFOV, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"
                     ToolTip="Camera vertical field of view in degrees. Default is 25."/>

            <!-- NEW: Euler angle controls (always visible) -->
            <TextBlock Text="Camera Pitch (degrees):" Margin="0,5,0,2"/>
            <TextBox Text="{Binding CamPitch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>

            <TextBlock Text="Camera Yaw (degrees):" Margin="0,0,0,2"/>
            <TextBox Text="{Binding CamYaw, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>

            <TextBlock Text="Camera Roll (degrees):" Margin="0,0,0,2"/>
            <TextBox Text="{Binding CamRoll, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>

        <!-- Portrait mode offsets (conditionally visible) -->
        <StackPanel>
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedCameraMode}" Value="Portrait">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="Head Top Offset (%):" Margin="0,0,0,2"/>
            <TextBox Text="{Binding HeadTopOffset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>
            <TextBlock Text="Head Bottom Offset (%):" Margin="0,0,0,2"/>
            <TextBox Text="{Binding HeadBottomOffset, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>
        </StackPanel>

        <!-- Fixed mode position (conditionally visible) -->
        <StackPanel>
            <StackPanel.Style>
                <Style TargetType="StackPanel">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedCameraMode}" Value="Fixed">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </StackPanel.Style>
            <TextBlock Text="Camera X:" Margin="0,0,0,2"/>
            <TextBox Text="{Binding CamX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>
            <TextBlock Text="Camera Y:" Margin="0,0,0,2"/>
            <TextBox Text="{Binding CamY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>
            <TextBlock Text="Camera Z:" Margin="0,0,0,2"/>
            <TextBox Text="{Binding CamZ, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5"
                     PreviewTextInput="FloatTextBox_PreviewTextInput"
                     DataObject.Pasting="FloatTextBox_Pasting"/>
        </StackPanel>
                            <TextBlock Text="Output Resolution:" Margin="0,10,0,2"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Text="{Binding ImageXRes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50"
                                         PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                         DataObject.Pasting="IntegerTextBox_Pasting"/>
                                <TextBlock Text=" x " VerticalAlignment="Center" Margin="5,0"/>
                                <TextBox Text="{Binding ImageYRes, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="50"
                                         PreviewTextInput="IntegerTextBox_PreviewTextInput"
                                         DataObject.Pasting="IntegerTextBox_Pasting"/>
                            </StackPanel>
                            
                        </StackPanel>
                    </Border>

                    <!-- Auto-generated mugshot maintenance — visible whenever
                         the auto-generation toggle is on, regardless of which
                         renderer is selected. Both Internal and Legacy stamp
                         the "Parameters" tEXt chunk that IsAutoGenerated
                         detects, so a single Delete button covers both. -->
                    <Grid Margin="15,10,0,5"
                          Visibility="{Binding UsePortraitCreatorFallback, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Content="Delete All Auto-Generated Mugshots"
                                Command="{Binding DeleteAutoGeneratedMugshotsCommand}"
                                Padding="10,5"
                                ToolTip="Permanently deletes all auto-generated portrait images and cleans up empty folders. Detects mugshots from both the Internal and Legacy renderers via the &quot;Parameters&quot; PNG metadata chunk.">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                    <Setter Property="Background" Value="#FFE57373"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#FFD32F2F"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Search Mode:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <ComboBox ItemsSource="{Binding MugshotSearchModes}"
                                      SelectedItem="{Binding SelectedMugshotSearchModePC, Mode=TwoWay}"
                                      MinWidth="120"
                                      ToolTip="Fast: Uses cached list of generated mugshots (recommended). Comprehensive: Scans all mugshot folders and reads metadata."/>
                        </StackPanel>
                    </Grid>

                    <CheckBox Content="Normalize Image Dimensions"
                              IsChecked="{Binding NormalizeImageDimensions, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="If checked, all images will be cropped and resized to the most common dimensions for a uniform appearance. This may slightly increase load times."/> 
                    
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="Max # Mugshots To Fit On Screen:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Text="{Binding MaxMugshotsToFit, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="50"
                                 ToolTip="The maximum number of mugshots to fit on-screen when auto-sizing. A lower number results in larger images."/>
                    </StackPanel>
                </StackPanel>
            </Expander>

            <!-- FaceGen Analysis: opt-in per-mugshot polycount / size overlay.
                 Off by default; turning it on triggers a NIF parse on every
                 visible tile that lacks cached stats — first view of each
                 mod's FaceGen costs ~10 ms. Subsequent views hit the
                 SHA-keyed cache in Settings.json. -->
            <Expander Header="FaceGen Analysis" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionFaceGenAnalysis.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <CheckBox Content="Enable FaceGen Analysis"
                              IsChecked="{Binding EnableFaceGenAnalysis, Mode=TwoWay}"
                              ToolTip="Analyze each tile's FaceGen NIF to show polycount / vertex / size info on the mugshot. First view of each FaceGen costs ~10ms; results are cached SHA-keyed across runs."/>
                    <TextBlock Text="When enabled, each mugshot's FaceGen NIF is parsed (or just stat'd for size-only) to surface authoring metrics. Useful for spotting absurdly heavy hair / head meshes."
                               FontSize="10" Foreground="{DynamicResource HintForeground}"
                               TextWrapping="Wrap" Margin="0,2,0,5"/>

                    <Border Margin="15,5,0,5" Padding="10" BorderBrush="Gray" BorderThickness="1" CornerRadius="3"
                            Visibility="{Binding EnableFaceGenAnalysis, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Border.Resources>
                            <!-- Same theme-aware DoubleUpDown style used by the Internal
                                 Renderer panel above; toolkit default template otherwise
                                 paints a system-light textbox interior that clashes with
                                 dark themes. -->
                            <Style TargetType="{x:Type xctk:DoubleUpDown}">
                                <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
                                <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="CaretBrush" Value="{DynamicResource PrimaryForeground}"/>
                            </Style>
                        </Border.Resources>
                        <StackPanel>
                            <TextBlock Text="Reporting Metrics" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <CheckBox Content="File Size" IsChecked="{Binding ReportFaceGenSize, Mode=TwoWay}" Margin="0,0,16,0"/>
                                <CheckBox Content="Polygons" IsChecked="{Binding ReportFaceGenPolys, Mode=TwoWay}" Margin="0,0,16,0"
                                          ToolTip="Triangle count summed across all shapes in the FaceGen NIF."/>
                                <CheckBox Content="Vertices" IsChecked="{Binding ReportFaceGenVerts, Mode=TwoWay}"
                                          ToolTip="Vertex count summed across all shapes."/>
                            </WrapPanel>

                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="Display Mode:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <ComboBox ItemsSource="{Binding FaceGenDisplayModes}"
                                          SelectedItem="{Binding FaceGenDisplayMode, Mode=TwoWay}"
                                          MinWidth="130"
                                          ToolTip="Text Overlay draws the numbers directly on the mugshot. Tooltip shows a small dot — hover for the same info."/>
                            </WrapPanel>

                            <!-- Text-overlay only: text-height % of mugshot height. -->
                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8"
                                       Visibility="{Binding FaceGenDisplayMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=TextOverlay}">
                                <TextBlock Text="Text Height (% of mugshot):" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <xctk:DoubleUpDown Value="{Binding FaceGenTextHeightPercent, Mode=TwoWay}"
                                                   Minimum="3" Maximum="30" Increment="0.5" FormatString="0.#" Width="70"/>
                            </WrapPanel>

                            <!-- Tooltip only: indicator position. -->
                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8"
                                       Visibility="{Binding FaceGenDisplayMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Tooltip}">
                                <TextBlock Text="Indicator Position:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                <ComboBox ItemsSource="{Binding FaceGenTooltipPositions}"
                                          SelectedItem="{Binding FaceGenTooltipPosition, Mode=TwoWay}"
                                          MinWidth="130"/>
                            </WrapPanel>

                            <TextBlock Text="Highlight Criterion" FontWeight="SemiBold" Margin="0,4,0,4"/>
                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <ComboBox ItemsSource="{Binding FaceGenHighlightCriteria}"
                                          SelectedItem="{Binding FaceGenHighlightCriterion, Mode=TwoWay}"
                                          MinWidth="130" Margin="0,0,8,0"
                                          ToolTip="Top % marks the heaviest N% of visible tiles per metric. Std Dev marks tiles whose value exceeds mean + N*stddev. Spectrum maps every tile along a Low→Mid→High gradient using min/max of visible tiles."/>
                                <xctk:DoubleUpDown Value="{Binding FaceGenHighlightThreshold, Mode=TwoWay}"
                                                   Minimum="0" Maximum="100" Increment="1" FormatString="0.##" Width="70"
                                                   Visibility="{Binding IsSpectrumMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}"/>
                            </WrapPanel>

                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8"
                                       Visibility="{Binding IsSpectrumMode, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Invert}">
                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                    <TextBlock Text="Highlight color:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <Border BorderBrush="Black" BorderThickness="1" Width="60" Height="22"
                                            Background="{Binding FaceGenHighlightColor}" CornerRadius="2"
                                            VerticalAlignment="Center"/>
                                    <Button Content="Change..." Margin="6,0,0,0" Padding="6,1"
                                            VerticalAlignment="Center"
                                            Command="{Binding SelectFaceGenHighlightColorCommand}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Normal color:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <Border BorderBrush="Black" BorderThickness="1" Width="60" Height="22"
                                            Background="{Binding FaceGenNoHighlightColor}" CornerRadius="2"
                                            VerticalAlignment="Center"/>
                                    <Button Content="Change..." Margin="6,0,0,0" Padding="6,1"
                                            VerticalAlignment="Center"
                                            Command="{Binding SelectFaceGenNoHighlightColorCommand}"/>
                                </StackPanel>
                            </WrapPanel>

                            <WrapPanel Orientation="Horizontal" Margin="0,0,0,8"
                                       Visibility="{Binding IsSpectrumMode, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                    <TextBlock Text="Low color:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <Border BorderBrush="Black" BorderThickness="1" Width="60" Height="22"
                                            Background="{Binding FaceGenSpectrumLowColor}" CornerRadius="2"
                                            VerticalAlignment="Center"/>
                                    <Button Content="Change..." Margin="6,0,0,0" Padding="6,1"
                                            VerticalAlignment="Center"
                                            Command="{Binding SelectFaceGenSpectrumLowColorCommand}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                    <TextBlock Text="Mid color:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <Border BorderBrush="Black" BorderThickness="1" Width="60" Height="22"
                                            Background="{Binding FaceGenSpectrumMidColor}" CornerRadius="2"
                                            VerticalAlignment="Center"/>
                                    <Button Content="Change..." Margin="6,0,0,0" Padding="6,1"
                                            VerticalAlignment="Center"
                                            Command="{Binding SelectFaceGenSpectrumMidColorCommand}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="High color:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                    <Border BorderBrush="Black" BorderThickness="1" Width="60" Height="22"
                                            Background="{Binding FaceGenSpectrumHighColor}" CornerRadius="2"
                                            VerticalAlignment="Center"/>
                                    <Button Content="Change..." Margin="6,0,0,0" Padding="6,1"
                                            VerticalAlignment="Center"
                                            Command="{Binding SelectFaceGenSpectrumHighColorCommand}"/>
                                </StackPanel>
                            </WrapPanel>

                            <Button Content="Clear FaceGen Analysis Cache"
                                    Command="{Binding ClearFaceGenAnalysisCacheCommand}"
                                    Padding="10,3" HorizontalAlignment="Left"
                                    ToolTip="Forgets every cached FaceGen stat. Next view of each tile will re-parse its NIF."/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Expander>
            
            <!-- Section: Display Settings -->
            <Expander Header="Display Settings" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionDisplaySettings.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <TextBlock Text="Theme:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox ItemsSource="{Binding AvailableThemes}"
                                  SelectedItem="{Binding SelectedThemeName, Mode=TwoWay}"
                                  MinWidth="160"/>
                        <TextBlock Text="Tab Style:" VerticalAlignment="Center" Margin="20,0,8,0"/>
                        <ComboBox ItemsSource="{Binding AvailableTabStyles}"
                                  SelectedItem="{Binding SelectedTabStyle, Mode=TwoWay}"
                                  MinWidth="100"/>
                        <TextBlock Text="NPC Selection Label:" VerticalAlignment="Center" Margin="20,0,8,0"/>
                        <ComboBox ItemsSource="{Binding AvailableNpcSelectionIndicators}"
                                  SelectedItem="{Binding SelectedNpcSelectionIndicator, Mode=TwoWay}"
                                  MinWidth="100"
                                  ToolTip="Show a colored indicator on NPCs with saved selections. Green = has data, Purple = mugshot only, Red = not in load order."/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <CheckBox Content="Auto-advance after selection" IsChecked="{Binding AutoAdvanceAfterSelection, Mode=TwoWay}"
                                  VerticalAlignment="Center"
                                  ToolTip="Automatically advance to the next NPC in the list after selecting a mugshot."/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <CheckBox Content="Enable Localization"
                                  IsChecked="{Binding IsLocalizationEnabled, Mode=TwoWay}"
                                  VerticalAlignment="Center"/>

                        <ComboBox ItemsSource="{Binding AvailableLanguages}"
                                  SelectedItem="{Binding SelectedLocalizationLanguage, Mode=TwoWay}"
                                  MinWidth="120"
                                  Margin="10,0,0,0"
                                  Visibility="{Binding IsLocalizationEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        
                        <CheckBox Margin="5 0 0 0" IsChecked="{Binding FixGarbledText}" 
                                  Visibility="{Binding IsLocalizationEnabled, Converter={StaticResource BooleanToVisibilityConverter}}"
                                  ToolTip="If your localized NPC names look garbled, try checking this box to enable the fix.">Fix Encoding</CheckBox>
                    </StackPanel>
                    
                    <CheckBox Content="Don't show me popup warnings for actions with potential side effects"
                              IsChecked="{Binding SuppressPopupWarnings, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="If checked, confirmation dialogs for potentially risky settings will be suppressed. Only check this if you're sure you understand what you're doing."/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="NPC List Display:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <CheckBox Content="Name" IsChecked="{Binding ShowNpcNameInList, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="Show the NPC's in-game name."/>
                        <CheckBox Content="EditorID" IsChecked="{Binding ShowNpcEditorIdInList, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="Show the NPC's editor ID."/>
                        <CheckBox Content="FormKey" IsChecked="{Binding ShowNpcFormKeyInList, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="Show the NPC's FormKey (e.g., 000123AB:Skyrim.esm)."/>
                        <CheckBox Content="FormID" IsChecked="{Binding ShowNpcFormIdInList, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,10,0" ToolTip="Show the NPC's full FormID based on your load order (e.g., 010123AB)."/>
                        <CheckBox Content="Template Status" IsChecked="{Binding ShowTemplateStatusInList, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,5,0"
                                  ToolTip="Show template status icons (T / !) next to NPCs that are used as appearance templates by other NPCs or mods."/>
                        <ComboBox ItemsSource="{Binding TemplateIconPositions}"
                                  SelectedItem="{Binding TemplateIconPosition, Mode=TwoWay}"
                                  MinWidth="60" VerticalAlignment="Center" Margin="0,0,10,0"
                                  ToolTip="Choose whether template icons appear to the left or right of the NPC name."
                                  Visibility="{Binding ShowTemplateStatusInList, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        <TextBlock Text="Separator:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Text="{Binding NpcListSeparator, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="30" VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
            </Expander>

            <!-- Section 4: EasyNPC Transfer -->
            <Expander Header="EasyNPC Transfer" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionEasyNpcTransfer.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <!-- Import Button -->
                    <Button Content="Import NPC Appearance Choices from EasyNPC Profile"
                            Command="{Binding ImportEasyNpcCommand}"
                            HorizontalAlignment="Left" Margin="0,0,0,5"
                            ToolTip="Import settings from a standard EasyNPC profile text file."/>

                    <!-- Export Button -->
                    <Button Content="Export NPC Appearance Choices To New EasyNPC Profile"
                            Command="{Binding ExportEasyNpcCommand}"
                            HorizontalAlignment="Left" Margin="0,0,0,5"
                            ToolTip="Export current settings to a new EasyNPC profile text file."/>

                    <!-- *** NEW: Update Profile Controls *** -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="Update Existing EasyNPC Profile"
                                Command="{Binding UpdateEasyNpcProfileCommand}"
                                CommandParameter="{Binding AddMissingNpcsOnUpdate}"
                                ToolTip="Update an existing EasyNPC profile file with current selections."
                                VerticalAlignment="Center"/>
                        <CheckBox Content="Add Missing NPCs?"
                                  IsChecked="{Binding AddMissingNpcsOnUpdate, Mode=TwoWay}"
                                  Margin="10,0,0,0"
                                  VerticalAlignment="Center"
                                  ToolTip="If checked, NPCs selected here but not found in the file will be added."/>
                    </StackPanel>
                    <!-- *** END: Update Profile Controls *** -->


                    <!-- NPC Default Plugin Exclusions (Unchanged) -->
                    <TextBlock Text="NPC Default Plugin Exclusions:" Margin="0,0,0,2"/>
                    <local:ModSelectorView DataContext="{Binding ExclusionSelectorViewModel}"
                                           Height="180"
                                           Margin="0,0,0,10"
                                           ToolTip="Select plugins to exclude when determining the 'default' appearance plugin in EasyNPC-like mode."/>
                    <TextBlock Text="Plugins selected here will be ignored when assigning conflict-winning overrides to EasyNPC's Default Plugin selections" FontSize="10" Foreground="{DynamicResource HintForeground}" TextWrapping="Wrap"/>
                </StackPanel>
            </Expander>
            <Expander Header="Load Order Import Settings" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionLoadOrderImport.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <TextBlock Text="Import Choices from Load Order Exclusions:" Margin="0,0,0,2"/>
                    <local:ModSelectorView DataContext="{Binding ImportFromLoadOrderExclusionSelectorViewModel}"
                                           Height="180"
                                           Margin="0,0,0,10"
                                           ToolTip="Select plugins to exclude when using the 'Import Choices from Load Order' feature."/>
                    <TextBlock Text="When importing choices from your load order, winning override plugins selected here will be ignored, and the next available override will be chosen instead." FontSize="10" Foreground="{DynamicResource HintForeground}" TextWrapping="Wrap"/>
                </StackPanel>
            </Expander>
            <Expander Header="Mod Import Settings" Style="{StaticResource SettingsSectionExpander}"
                      Margin="0,5,0,10"
                      IsExpanded="{Binding SectionModImport.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <TextBlock Text="Non-Appearance Mods:" Margin="0,0,0,2"/>

                    <TextBlock Text="The highlighted mods could not be scanned due to missing masters. Install the listed masters as separate mod folders and click the refresh button to re-scan."
                               Foreground="{DynamicResource StatusErrorForeground}"
                               TextWrapping="Wrap"
                               FontStyle="Italic"
                               FontSize="10"
                               Margin="0,0,0,4"
                               Visibility="{Binding HasMissingMasterMods, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                    <TextBox Text="{Binding NonAppearanceModFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,3" ToolTip="Filter the list of non-appearance mods below."/>

                          <ListBox ItemsSource="{Binding FilteredNonAppearanceMods}" Height="150" HorizontalContentAlignment="Stretch" BorderThickness="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding Path, Converter={StaticResource FullPathToFolderNameConverter}}"
                                       ToolTip="{Binding TooltipText}"
                                       VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMissingMasters}" Value="True">
                                                <Setter Property="Foreground" Value="{DynamicResource StatusErrorForeground}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <Button Grid.Column="1"
                                    Content="🔄"
                                    Command="{Binding DataContext.RescanCachedModCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding Path}"
                                    ToolTip="Re-scan this folder to check if it is now an appearance mod"
                                    FontWeight="Bold"
                                    Cursor="Hand"
                                    Margin="10,0,0,0"
                                    VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                             <Button Grid.Column="2"
                                    Content="X"
                                    Command="{Binding DataContext.RemoveCachedModCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding Path}"
                                    Foreground="{DynamicResource StatusErrorForeground}"
                                    FontWeight="Bold"
                                    Cursor="Hand"
                                    Margin="5,0,0,0"
                                    VerticalAlignment="Center">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="Transparent"/>
                                        <Setter Property="BorderThickness" Value="0"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

                    <TextBlock Text="These mods have been auto-determined to not provide NPC replacers.
Mouse over each entry to see the reason.
Delete a mod if you want to re-scan it."
                               FontSize="10" 
                               Foreground="{DynamicResource HintForeground}" 
                  
             TextWrapping="Wrap"
                               Margin="0,2,0,0" />

                    <Separator Margin="0,12,0,8"/>

                    <TextBlock Text="Ignored Mods:" Margin="0,0,0,2"/>
                    <TextBox Text="{Binding IgnoredModFilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,3" ToolTip="Filter the list of ignored mods below."/>

                    <ListBox ItemsSource="{Binding FilteredIgnoredMods}" Height="150" HorizontalContentAlignment="Stretch" BorderThickness="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Converter={StaticResource FullPathToFolderNameConverter}}"
                                               ToolTip="{Binding}"
                                               VerticalAlignment="Center"/>

                                    <Button Grid.Column="1"
                                            Content="X"
                                            Command="{Binding DataContext.RemoveIgnoredModCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Foreground="{DynamicResource StatusErrorForeground}"
                                            FontWeight="Bold"
                                            Cursor="Hand"
                                            Margin="5,0,0,0"
                                            VerticalAlignment="Center">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Button Content="Add Folder(s)..."
                            Command="{Binding AddIgnoredModCommand}"
                            Margin="0,4,0,0"
                            HorizontalAlignment="Left"
                            Padding="10,3"
                            ToolTip="Select folder(s) to ignore during mod import"/>
                    <TextBlock Text="Mods added here will be skipped during import. If a mod is already loaded, it will be removed.
Remove a mod from this list to allow it to be imported again."
                               FontSize="10"
                               Foreground="{DynamicResource HintForeground}"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0" />

                    <Separator Margin="0,12,0,8"/>

                    <!-- Rejected NPCs viewer. Nested rather than a top-level section because it
                         explains the outcome of the mod import the rest of this section configures.
                         Reads the "Rejected NPCs" folder written during mod analysis; the read is
                         deferred until this expander is first opened (see VM_Settings). -->
                    <Expander Header="Rejected NPCs" Style="{StaticResource SettingsSectionExpander}"
                              Margin="0,0,0,0"
                              IsExpanded="{Binding SectionRejectedNpcs.IsExpanded, Mode=TwoWay}">
                        <StackPanel DataContext="{Binding RejectedNpcs}">
                            <TextBlock TextWrapping="Wrap" FontSize="10"
                                       Foreground="{DynamicResource HintForeground}"
                                       Margin="0,0,0,6"
                                       Text="NPCs that were discarded while analyzing your mods, and why. Select a mod or an NPC on the left to see the reason on the right. Re-scan your mods and click Refresh to bring this up to date."/>

                            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                <Button Content="Refresh"
                                        Command="{Binding RefreshCommand}"
                                        Padding="10,3"
                                        ToolTip="Re-read the 'Rejected NPCs' folder."/>
                                <Button Content="Open Folder"
                                        Command="{Binding OpenFolderCommand}"
                                        Padding="10,3" Margin="6,0,0,0"
                                        ToolTip="Open the 'Rejected NPCs' folder (next to the application) in Explorer."/>
                                <TextBlock Text="{Binding StatusText}"
                                           VerticalAlignment="Center" Margin="10,0,0,0"
                                           TextWrapping="Wrap"
                                           Foreground="{DynamicResource HintForeground}"/>
                            </StackPanel>

                            <TextBox Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, Delay=250}"
                                     Margin="0,0,0,3"
                                     ToolTip="Filter by mod name, NPC name / EditorID / FormKey, or rejection reason."/>

                            <Grid Height="320">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="280" MinWidth="140"/>
                                    <ColumnDefinition Width="5"/>
                                    <ColumnDefinition Width="*" MinWidth="180"/>
                                </Grid.ColumnDefinitions>

                                <TreeView Grid.Column="0"
                                          ItemsSource="{Binding Groups}"
                                          Background="{DynamicResource TextBoxBackground}"
                                          Foreground="{DynamicResource PrimaryForeground}"
                                          BorderBrush="{DynamicResource BorderBrush}"
                                          BorderThickness="1"
                                          VirtualizingStackPanel.IsVirtualizing="True"
                                          VirtualizingStackPanel.VirtualizationMode="Standard"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          SelectedItemChanged="RejectedNpcsTree_SelectedItemChanged">
                                    <TreeView.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel/>
                                        </ItemsPanelTemplate>
                                    </TreeView.ItemsPanel>
                                    <!-- Declared explicitly rather than as DataType-keyed resources:
                                         the tree is exactly two fixed levels, and naming the
                                         templates outright means the hierarchy (and so each mod
                                         row's expand chevron) never depends on implicit template
                                         lookup succeeding for a recycled container.

                                         Rows ellipsize instead of scrolling sideways: horizontal
                                         scrolling is disabled above because selecting a row raises
                                         BringIntoView, and with mod names wider than this pane that
                                         scrolled the tree right by the width of the chevron column,
                                         putting every expander arrow out of reach. Full text is in
                                         the tooltip and in the detail pane. -->
                                    <TreeView.ItemTemplate>
                                        <HierarchicalDataTemplate ItemsSource="{Binding Entries}">
                                            <TextBlock Text="{Binding DisplayText}"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding ModName}"/>
                                            <HierarchicalDataTemplate.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding DisplayText}"
                                                               TextTrimming="CharacterEllipsis"
                                                               ToolTip="{Binding NpcIdentifier}"/>
                                                </DataTemplate>
                                            </HierarchicalDataTemplate.ItemTemplate>
                                        </HierarchicalDataTemplate>
                                    </TreeView.ItemTemplate>
                                    <TreeView.Resources>
                                        <!-- Retemplated here rather than per theme, like the section
                                             Expander above: the default TreeViewItem paints its
                                             selection with SystemColors, which reads as a light-mode
                                             control in the dark themes. Implicit (unkeyed) so it
                                             reaches items at both levels of the tree. -->
                                        <Style TargetType="TreeViewItem">
                                            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                                            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                                            <Setter Property="Padding" Value="2,2"/>
                                            <Setter Property="ItemsPanel">
                                                <Setter.Value>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel/>
                                                    </ItemsPanelTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="TreeViewItem">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="16"/>
                                                                <ColumnDefinition Width="*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="Auto"/>
                                                                <RowDefinition/>
                                                            </Grid.RowDefinitions>

                                                            <ToggleButton x:Name="Expander"
                                                                          Grid.Row="0" Grid.Column="0"
                                                                          IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                                          ClickMode="Press" Focusable="False">
                                                                <ToggleButton.Template>
                                                                    <ControlTemplate TargetType="ToggleButton">
                                                                        <Border Background="Transparent" Width="16" Height="16">
                                                                            <Path x:Name="Chevron"
                                                                                  Data="F1 M 0,0 L 5,0 L 2.5,3 Z"
                                                                                  Stretch="Uniform" Width="7" Height="7"
                                                                                  Fill="{DynamicResource PrimaryForeground}"
                                                                                  HorizontalAlignment="Center" VerticalAlignment="Center"
                                                                                  RenderTransformOrigin="0.5,0.5"/>
                                                                        </Border>
                                                                        <ControlTemplate.Triggers>
                                                                            <Trigger Property="IsChecked" Value="False">
                                                                                <Setter TargetName="Chevron" Property="RenderTransform">
                                                                                    <Setter.Value>
                                                                                        <RotateTransform Angle="-90"/>
                                                                                    </Setter.Value>
                                                                                </Setter>
                                                                            </Trigger>
                                                                        </ControlTemplate.Triggers>
                                                                    </ControlTemplate>
                                                                </ToggleButton.Template>
                                                            </ToggleButton>

                                                            <Border x:Name="Bd"
                                                                    Grid.Row="0" Grid.Column="1"
                                                                    Background="Transparent"
                                                                    CornerRadius="2"
                                                                    Padding="{TemplateBinding Padding}"
                                                                    SnapsToDevicePixels="True">
                                                                <ContentPresenter x:Name="PART_Header"
                                                                                  ContentSource="Header"
                                                                                  HorizontalAlignment="Left"
                                                                                  VerticalAlignment="Center"/>
                                                            </Border>

                                                            <ItemsPresenter x:Name="ItemsHost"
                                                                            Grid.Row="1" Grid.Column="1"/>
                                                        </Grid>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsExpanded" Value="False">
                                                                <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                                            </Trigger>
                                                            <Trigger Property="HasItems" Value="False">
                                                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Bd" Property="Background"
                                                                        Value="{DynamicResource SelectedItemBackground}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </TreeView.Resources>
                                </TreeView>

                                <GridSplitter Grid.Column="1" Width="5"
                                              HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                              Background="Transparent"/>

                                <Border Grid.Column="2"
                                        Background="{DynamicResource TextBoxBackground}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        Padding="8">
                                    <Grid>
                                        <TextBlock Text="Select a mod or an NPC on the left to see why it was rejected."
                                                   Foreground="{DynamicResource HintForeground}"
                                                   TextWrapping="Wrap"
                                                   Visibility="{Binding HasNoSelection, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                      Visibility="{Binding HasSelection, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <StackPanel DataContext="{Binding SelectedNode}">
                                                <TextBlock Text="{Binding DetailTitle}"
                                                           FontWeight="Bold" FontSize="14" TextWrapping="Wrap"/>
                                                <TextBlock Text="{Binding DetailSubtitle}"
                                                           Foreground="{DynamicResource HintForeground}"
                                                           TextWrapping="Wrap" Margin="0,2,0,8"/>
                                                <!-- Read-only TextBoxes rather than TextBlocks so the
                                                     reason and the raw log line can be copied out. -->
                                                <TextBox Text="{Binding DetailReason, Mode=OneWay}"
                                                         IsReadOnly="True" TextWrapping="Wrap"
                                                         Background="Transparent" BorderThickness="0" Padding="0"
                                                         Foreground="{DynamicResource PrimaryForeground}"/>
                                                <TextBox Text="{Binding DetailMeta, Mode=OneWay}"
                                                         IsReadOnly="True" TextWrapping="Wrap"
                                                         Background="Transparent" BorderThickness="0" Padding="0"
                                                         Margin="0,10,0,0" FontSize="10"
                                                         Foreground="{DynamicResource HintForeground}"/>
                                            </StackPanel>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </Expander>

            <Expander Header="Spawn Bat File Options" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionSpawnBatFile.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <TextBlock Text="Console Commands Before Spawning:" Margin="0,0,0,2"/>
                    <TextBox 
                        Text="{Binding BatFilePreCommands, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        Height="80"
                        VerticalScrollBarVisibility="Auto"
                        ToolTip="Enter any console commands you want to execute BEFORE the player.placeatme commands in the generated file."/>

                    <TextBlock Text="Console Commands After Spawning:" Margin="0,10,0,2"/>
                    <TextBox 
                        Text="{Binding BatFilePostCommands, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        AcceptsReturn="True"
                        TextWrapping="Wrap"
                        Height="80"
                        VerticalScrollBarVisibility="Auto"
                        ToolTip="Enter any console commands you want to execute AFTER the player.placeatme commands."/>
                </StackPanel>
            </Expander>
            
            <Expander Header="Logging" Style="{StaticResource SettingsSectionExpander}"
                      IsExpanded="{Binding SectionLogging.IsExpanded, Mode=TwoWay}">
                <StackPanel>
                    <CheckBox Content="Log Activity"
                              IsChecked="{Binding LogActivity, Mode=TwoWay}"
                              ToolTip="Enables detailed logging of application activity to EventLog.html (next to the .exe) to help diagnose issues. Open the file in any web browser."/>
                    <CheckBox Content="Log Startup"
                              IsChecked="{Binding LogStartup, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="Logs detailed startup operations to StartupLog.html on next launch (open it in any web browser). Useful for diagnosing startup hangs. Can also be triggered by placing a LogStartup.txt file next to the .exe."/>
                    <CheckBox Content="Log Record Provenance"
                              IsChecked="{Binding LogRecordProvenance, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="When on, a patch run writes RecordProvenance.csv next to the .exe: one row per record merged into the output plugin (excluding the patched NPC records themselves) — its FormKey, EditorID, record type, and the full chain of references that pulled it in, from the root NPC down to the record. Open in a spreadsheet and sort/filter by any column. Takes effect on the next Run — no restart needed. Useful for tracing why a particular record ended up in your output plugin."/>
                    <CheckBox Content="Log Asset Provenance"
                              IsChecked="{Binding LogAssetProvenance, Mode=TwoWay}"
                              Margin="0,5,0,0"
                              ToolTip="When on, a patch run writes AssetProvenance.csv next to the .exe: one row per asset file copied into the output — which NPC, mod, and (for plugin-referenced assets) which record pulled it in, plus its source (loose or BSA). Open in a spreadsheet and sort/filter by any column. Takes effect on the next Run — no restart needed. Useful for tracing why a particular texture/mesh ended up in your output."/>

                    <Separator Margin="0,10,0,8"/>

                    <TextBlock Text="Per-NPC Patch / Validation Logs" FontWeight="Bold"/>
                    <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,2,0,6"
                               Text="For each NPC in the list below, the Validator and Patcher write a full activity trace — every change to the NPC record plus the dependency merge-in logic (discovered records, abort conditions) — to 'NPC Logs\{NPC}.html' next to the application (open it in any web browser). Files are overwritten at the start of each run. Search the NPCs you've made a selection for (by name, EditorID, or FormKey) and click a result to add it."/>

                    <TextBox Text="{Binding NpcLogSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Type to search NPCs you've made a selection for, by name, EditorID, or FormKey."/>

                    <!-- Clickable search matches -->
                    <ListBox ItemsSource="{Binding NpcLogSearchResults}"
                             Visibility="{Binding HasNpcLogSearchResults, Converter={StaticResource BooleanToVisibilityConverter}}"
                             MaxHeight="160" HorizontalContentAlignment="Stretch"
                             BorderThickness="1" Margin="0,2,0,0"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button HorizontalContentAlignment="Left"
                                        Background="Transparent" BorderThickness="0" Cursor="Hand"
                                        Padding="2,1"
                                        Command="{Binding DataContext.AddNpcToLogCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"
                                        ToolTip="Click to add this NPC to the per-NPC log list">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis"/>
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <!-- Editable list of NPCs to log -->
                    <ListBox ItemsSource="{Binding LoggedNpcs}" Height="150" Margin="0,6,0,0"
                             HorizontalContentAlignment="Stretch" BorderThickness="1"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding DisplayName}"
                                               TextTrimming="CharacterEllipsis"
                                               VerticalAlignment="Center">
                                        <TextBlock.ToolTip>
                                            <ToolTip>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding NpcName}"/>
                                                    <TextBlock Text="{Binding NpcEditorId}"/>
                                                    <TextBlock Text="{Binding NpcFormKeyString}"/>
                                                </StackPanel>
                                            </ToolTip>
                                        </TextBlock.ToolTip>
                                    </TextBlock>

                                    <Button Grid.Column="1"
                                            Content="X"
                                            Command="{Binding DataContext.RemoveNpcFromLogCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}"
                                            Foreground="{DynamicResource StatusErrorForeground}"
                                            FontWeight="Bold"
                                            Cursor="Hand"
                                            Margin="5,0,0,0"
                                            VerticalAlignment="Center"
                                            ToolTip="Remove this NPC from the per-NPC log list">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="BorderThickness" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Expander>
        </StackPanel>
    </ScrollViewer>
</reactiveui:ReactiveUserControl>
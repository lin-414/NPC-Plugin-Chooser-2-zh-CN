<reactiveui:ReactiveUserControl x:Class="NPC_Plugin_Chooser_2.Views.SummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:NPC_Plugin_Chooser_2.Views"
             xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
             xmlns:reactiveui="http://reactiveui.net"
             xmlns:l="clr-namespace:NPC_Plugin_Chooser_2.Localization"
             mc:Ignorable="d"
             x:TypeArguments="vm:VM_Summary"
             d:DataContext="{d:DesignInstance Type=vm:VM_Summary}"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.Resources>
        <local:EnumMatchToBooleanConverter x:Key="EnumMatchToBooleanConverter"/>
        <local:EnumToVisibilityConverter x:Key="EnumToVisibilityConverter"/>
        <local:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:WidthPercentageConverter x:Key="WidthPercentageConverter"/> 
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="Gray" BorderThickness="0,0,0,1" Padding="5">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="{l:Loc viewMode}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <RadioButton Content="{l:Loc gallery}" GroupName="{l:Loc viewMode2}" VerticalAlignment="Center" Margin="0,0,5,0"
                             IsChecked="{Binding ViewMode, Converter={StaticResource EnumMatchToBooleanConverter}, ConverterParameter=Gallery}"/>
            
                <RadioButton Content="{l:Loc list}" GroupName="{l:Loc viewMode2}" VerticalAlignment="Center" Margin="0,0,15,0"
                             IsChecked="{Binding ViewMode, Converter={StaticResource EnumMatchToBooleanConverter}, ConverterParameter=List}"/>

                <TextBlock Text="{l:Loc nPCGroup}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <ComboBox MinWidth="150" ItemsSource="{Binding AvailableNpcGroups}" SelectedItem="{Binding SelectedNpcGroup, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,15,0"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding ViewMode, Converter={StaticResource EnumMatchToBooleanConverter}, ConverterParameter=Gallery}">
                    <TextBlock Text="{l:Loc maxNPCsPerPage}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding MaxNpcsPerPage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" MinWidth="50" VerticalAlignment="Center"/>
                </StackPanel>
            </WrapPanel>
        </Border>

        <Grid Grid.Row="1">
            <ScrollViewer x:Name="MugshotScrollViewer" Grid.Row="1"
                          VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                          PreviewMouseWheel="MugshotScrollViewer_PreviewMouseWheel"
                          SizeChanged="MugshotScrollViewer_SizeChanged"
                          Visibility="{Binding ViewMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Gallery}">
                <ItemsControl x:Name="SummaryItemsControl" ItemsSource="{Binding DisplayedItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"
                                       Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ScrollViewer}}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type vm:VM_SummaryMugshot}">
                            <Border BorderThickness="2" BorderBrush="Gray" Margin="5" CornerRadius="2"
                                    Width="{Binding ImageWidth}" Height="{Binding ImageHeight}">
                                <Border.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="{l:Loc showFullImage}" Command="{Binding ToggleFullScreenCommand}"/>
                                        <MenuItem Header="{l:Loc jumpToNPCIn}" Command="{Binding JumpToNpcCommand}"/>
                                        <MenuItem Header="{l:Loc jumpToModIn}" Command="{Binding JumpToModCommand}"/>
                                        <MenuItem Header="{l:Loc visitModPage}"
                                                  DisplayMemberPath="DisplayName"
                                                  Visibility="{Binding CanVisitModPage, Converter={StaticResource BooleanToVisibilityConverter}}">
                                            <MenuItem.Style>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Setter Property="Command" Value="{Binding VisitModPageCommand}"/>
                                                    <Setter Property="CommandParameter" Value="{Binding ModPageUrls[0].Url}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasSingleModPage}" Value="False">
                                                            <Setter Property="Command" Value="{x:Null}"/>
                                                            <Setter Property="CommandParameter" Value="{x:Null}"/>
                                                            <Setter Property="ItemsSource" Value="{Binding ModPageUrls}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </MenuItem.Style>
                                            <MenuItem.ItemContainerStyle>
                                                <Style TargetType="MenuItem" BasedOn="{StaticResource {x:Type MenuItem}}">
                                                    <Setter Property="Command" Value="{Binding DataContext.VisitModPageCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                                    <Setter Property="CommandParameter" Value="{Binding Url}"/>
                                                </Style>
                                            </MenuItem.ItemContainerStyle>
                                        </MenuItem>
                                    </ContextMenu>
                                </Border.ContextMenu>

                                <Grid Background="Black">
                                    <Image x:Name="MugshotImage"
                                           Source="{Binding MugshotSource}" Stretch="UniformToFill" UseLayoutRounding="True" SnapsToDevicePixels="True">
                                    </Image>
                                    <Border VerticalAlignment="Bottom" HorizontalAlignment="Stretch" Background="{DynamicResource MugshotCaptionBackground}" Padding="3,1">
                                        <StackPanel HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding NpcDisplayName}" Foreground="{DynamicResource MugshotCaptionForeground}"  FontSize="10" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>
                                            <Viewbox Stretch="Uniform" StretchDirection="DownOnly"
                                                     Width="{Binding ElementName=MugshotImage, Path=ActualWidth, Converter={StaticResource WidthPercentageConverter}, ConverterParameter=90}">
                                                <TextBlock Text="{Binding ModDisplayName}" Foreground="{DynamicResource MugshotCaptionSecondaryForeground}" FontSize="9" HorizontalAlignment="Center" FontStyle="Italic" TextAlignment="Center" TextWrapping="NoWrap"/>
                                            </Viewbox>
                                        </StackPanel>
                                    </Border>
                                    
                                    <Image Source="/NPC Plugin Chooser 2;component/Resources/RedDragon.png"
                                           Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           RenderTransformOrigin="0.5,0.5"
                                           Width="{Binding RelativeSource={RelativeSource AncestorType=Grid}, Path=ActualWidth, Converter={StaticResource WidthPercentageConverter}, ConverterParameter='50'}"
                                           Height="{Binding RelativeSource={RelativeSource Self}, Path=Width}">
                                        <Image.Style>
                                            <Style TargetType="Image">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard Name="OrbitDragon_BeginStoryboard">
                                                                <Storyboard>
                                                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[1].Angle" From="0" To="360" Duration="0:0:3" RepeatBehavior="Forever" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <StopStoryboard BeginStoryboardName="OrbitDragon_BeginStoryboard"/>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <TransformGroup>
                                                            <TranslateTransform/>
                                                            <RotateTransform/>
                                                        </TransformGroup>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Image.Style>
                                    </Image>
                                    
                                    </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <DataGrid ItemsSource="{Binding DisplayedItems}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      GridLinesVisibility="All"
                      HeadersVisibility="Column"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      Visibility="{Binding ViewMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=List}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{l:Loc nPC}" Binding="{Binding NpcDisplayName}" Width="2*"/>
                    <DataGridTextColumn Header="{l:Loc selectedMod}" Binding="{Binding SelectedModName}" Width="2*"/>

                    <DataGridTextColumn Header="{l:Loc sourceNPCIfDifferent}" Binding="{Binding SourceNpcForDisplay}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

        </Grid>

        <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="0,1,0,0" Padding="5">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center"
                            Visibility="{Binding ViewMode, Converter={StaticResource EnumToVisibilityConverter}, ConverterParameter=Gallery}">
                    <Button Content="{l:Loc lt}" Command="{Binding PreviousPageCommand}" Width="25" Height="25" Margin="0,0,5,0" ToolTip="{l:Loc previousPage}"/>
                    <TextBlock Text="{Binding PageDisplay}" VerticalAlignment="Center" Margin="5,0"/>
                    <Button Content="{l:Loc gt}" Command="{Binding NextPageCommand}" Width="25" Height="25" Margin="5,0,0,0" ToolTip="{l:Loc nextPage}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="ZoomOutButton" Content="-" Command="{Binding ZoomOutSummaryCommand}" Width="25" Height="25" Margin="2,0" ToolTip="{l:Loc zoomOutCtrlScroll}"/>
                    <TextBox x:Name="ZoomPercentageTextBox" Width="60" Height="25" Margin="2,0" TextAlignment="Center" VerticalContentAlignment="Center"
                             Text="{Binding SummaryViewZoomLevel, StringFormat='F2', Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             PreviewMouseWheel="ZoomPercentageTextBox_PreviewMouseWheel"
                             ToolTip="{l:Loc currentZoomLevel}"/>
                    <TextBlock Text="%" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <Button x:Name="ZoomInButton" Content="+" Command="{Binding ZoomInSummaryCommand}" Width="25" Height="25" Margin="2,0" ToolTip="{l:Loc zoomInCtrlScroll}"/>
                    <CheckBox x:Name="LockZoomCheckBox" Content="{l:Loc lockZoom}" IsChecked="{Binding SummaryViewIsZoomLocked, Mode=TwoWay}" Margin="10,0,5,0" VerticalAlignment="Center" ToolTip="{l:Loc lockTheCurrentZoom}"/>
                    <Button x:Name="ResetZoomButton" Content="{l:Loc resetZoom}" Command="{Binding ResetZoomSummaryCommand}" Margin="5,0,0,0" Padding="5,2" ToolTip="{l:Loc resetZoomToAutomatically}"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</reactiveui:ReactiveUserControl>
<!-- MainWindow.xaml -->
<reactiveui:ReactiveWindow x:Class="NPC_Plugin_Chooser_2.Views.MainWindow"
                           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                           xmlns:local="clr-namespace:NPC_Plugin_Chooser_2.Views"
                           xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
                           xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
                           xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                           xmlns:reactiveui="http://reactiveui.net"
                           mc:Ignorable="d"
                           x:TypeArguments="vm:VM_MainWindow"
                           d:DataContext="{d:DesignInstance Type=vm:VM_MainWindow}"
                           Title="NPC Plugin Chooser" Height="700" Width="1000"
                           Style="{DynamicResource ThemedWindowStyle}">
    
    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>
    
    <Window.Resources>
        <!-- Box-style tab template (original) -->
        <ControlTemplate x:Key="BoxTabTemplate" TargetType="RadioButton">
            <Border x:Name="Border"
                    Background="{DynamicResource ToggleButtonBackground}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,1,1,0"
                    CornerRadius="3,3,0,0"
                    Padding="10,4">
                <TextBlock Text="{TemplateBinding Content}"
                           Foreground="{TemplateBinding Foreground}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Border" Property="Background" Value="{DynamicResource PrimaryBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter TargetName="Border" Property="Panel.ZIndex" Value="1"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <!-- Underline-indicator tab template -->
        <ControlTemplate x:Key="UnderlineTabTemplate" TargetType="RadioButton">
            <Grid>
                <Border x:Name="HoverBg"
                        Background="{DynamicResource TertiaryBackground}"
                        CornerRadius="4"
                        Opacity="0"
                        Margin="2,4,2,6"/>
                <TextBlock x:Name="Label"
                           Text="{TemplateBinding Content}"
                           Foreground="{TemplateBinding Foreground}"
                           FontFamily="Segoe UI, sans-serif"
                           FontSize="13"
                           Padding="14,8,14,10"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                <Border x:Name="Underline"
                        Height="2"
                        CornerRadius="1"
                        Background="{DynamicResource AccentBrush}"
                        VerticalAlignment="Bottom"
                        Margin="10,0,10,0"
                        Opacity="0"/>
            </Grid>
            <ControlTemplate.Triggers>
                <EventTrigger RoutedEvent="UIElement.MouseEnter">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBg"
                                             Storyboard.TargetProperty="Opacity"
                                             To="1" Duration="0:0:0.10"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <EventTrigger RoutedEvent="UIElement.MouseLeave">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetName="HoverBg"
                                             Storyboard.TargetProperty="Opacity"
                                             To="0" Duration="0:0:0.12"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="Label"     Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter TargetName="Underline" Property="Opacity"    Value="1"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="TabToggleButtonStyle" TargetType="RadioButton">
            <Setter Property="Background"  Value="Transparent"/>
            <Setter Property="Foreground"  Value="{DynamicResource SecondaryForeground}"/>
            <Setter Property="Cursor"      Value="Hand"/>
            <Setter Property="Template"    Value="{StaticResource BoxTabTemplate}"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Tab Navigation -->
            <RowDefinition Height="*"/> <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Tab Navigation Bar -->
        <Border Grid.Row="0"
                Background="{DynamicResource NavBarBackground}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1">
        <!-- The tab radios carry NO {Binding}s: MainWindow's DataContext is never set, so a
             XAML binding here never resolves — and at Loaded, WPF re-evaluates the unresolved
             binding and resets the property to its default, stomping the value the code-behind
             this.Bind(...) wiring (MainWindow.xaml.cs, WhenActivated) just pushed and feeding
             false back into the VM. That is exactly the "no tab underlined on startup" bug
             pinned by MainWindowStartupUnderlineTests. IsChecked / IsEnabled are owned solely
             by the code-behind. -->
        <StackPanel Orientation="Horizontal" Margin="4,0">

          <RadioButton x:Name="NpcsRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="NPCs"
                       GroupName="MainTabs" />

          <RadioButton x:Name="ModsRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="Mods"
                       GroupName="MainTabs" />

          <RadioButton x:Name="ModIssuesRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="Mod Issues"
                       GroupName="MainTabs" />

          <RadioButton x:Name="SummaryRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="Summary"
                       GroupName="MainTabs" />

          <RadioButton x:Name="SettingsRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="Settings"
                       GroupName="MainTabs" />

          <RadioButton x:Name="RunRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="Run"
                       GroupName="MainTabs" />

          <RadioButton x:Name="ValidateRadioButton"
                       Style="{StaticResource TabToggleButtonStyle}"
                       Content="Validate"
                       GroupName="MainTabs" />

        </StackPanel>
        </Border>

        <!-- Content Area -->
        <reactiveui:ViewModelViewHost Grid.Row="1"
                                      x:Name="ViewModelViewHost"
                                      ViewModel="{Binding CurrentViewModel}"
                                      VerticalAlignment="Stretch"
                                      HorizontalAlignment="Stretch"
                                      HorizontalContentAlignment="Stretch"
                                      VerticalContentAlignment="Stretch"
                                      Margin="0"/>
    </Grid>
</reactiveui:ReactiveWindow>
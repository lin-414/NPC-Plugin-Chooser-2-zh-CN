<!-- ShowInTaskbar is True here, unlike the other owned dialogs: this one is modal and can sit
     open for a long time while the user reads the list. Without a taskbar button of its own,
     switching to another app and back lands on the blocked main window, which just looks frozen
     with nothing to indicate a dialog is waiting behind it. -->
<Window x:Class="NPC_Plugin_Chooser_2.Views.InvalidSelectionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
        xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:l="clr-namespace:NPC_Plugin_Chooser_2.Localization"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:VM_InvalidSelectionsWindow}"
        Title="Invalid Selections Found"
        Height="620" Width="1060"
        MinHeight="360" MinWidth="640"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True"
        Style="{DynamicResource ThemedWindowStyle}">

    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SummaryText}"
                   FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <!-- Filter -->
        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Filter:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="1"
                     Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     ToolTip="Filter rows by any column"/>
        </Grid>

        <!-- One row per rejected NPC, sortable by any column. Cell selection + ExcludeHeader:
             highlight any run of cells (drag, or Ctrl/Shift-click) and Ctrl+C pastes just those
             cells, tab-separated, with no column header line; the buttons below are what emit a
             full headered table. Styled inline like ValidationResultsWindow — the theme files
             carry NO implicit DataGrid styles. -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding FilteredRows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Extended"
                  SelectionUnit="CellOrRowHeader"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  ClipboardCopyMode="ExcludeHeader"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  Background="{DynamicResource SecondaryBackground}"
                  Foreground="{DynamicResource PrimaryForeground}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  RowBackground="{DynamicResource SecondaryBackground}"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                  VerticalGridLinesBrush="{DynamicResource BorderBrush}">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Copy selected cells"
                              Command="ApplicationCommands.Copy"
                              InputGestureText="Ctrl+C"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="6,4"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Issue" Binding="{Binding Reason}" Width="2*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Mod" Binding="{Binding ModName}" Width="1.5*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="NPC" Binding="{Binding NpcDescription}" Width="1.2*"/>
                <DataGridTextColumn Header="FormKey" Binding="{Binding NpcFormKey}" Width="160"/>
                <DataGridTextColumn Header="Missing Record" Binding="{Binding Detail}" Width="1.5*">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Margin="0,10,0,0" TextWrapping="Wrap"
                   Text="Skip them and continue with only the valid selections?"/>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="{l:Loc copyText}" Command="{Binding CopyTextCommand}"
                        Height="30" Padding="12,0"
                        ToolTip="{l:Loc copyTheWholeList}"/>
                <Button Content="{l:Loc copySheet}" Command="{Binding CopySheetCommand}"
                        Height="30" Padding="12,0" Margin="10,0,0,0"
                        ToolTip="{l:Loc copyOneTabSeparated}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{l:Loc skipAndContinue}" Click="ContinueButton_Click"
                        Height="30" Padding="12,0" IsDefault="True"
                        ToolTip="{l:Loc patchTheValidSelections}"/>
                <Button Content="{l:Loc cancelRun}" Click="CancelButton_Click"
                        Height="30" Padding="12,0" Margin="10,0,0,0" IsCancel="True"
                        ToolTip="{l:Loc abortWithoutPatchingAnything}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

<!-- ShowInTaskbar is True here, unlike the other owned dialogs: this one is modal and can sit
     open for a long time while the user reads the list. Without a taskbar button of its own,
     switching to another app and back lands on the blocked main window, which just looks frozen
     with nothing to indicate a dialog is waiting behind it. -->
<Window x:Class="NPC_Plugin_Chooser_2.Views.InvalidSelectionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
        xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:l="clr-namespace:NPC_Plugin_Chooser_2.Localization"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:VM_InvalidSelectionsWindow}"
        Title="{l:Loc invalidSelectionsFound}"
        Height="620" Width="860"
        MinHeight="360" MinWidth="560"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True"
        Style="{DynamicResource ThemedWindowStyle}">

    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SummaryText}"
                   FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <!-- Issue -> mod -> NPC. Issues open, mods shut: one shared reason routinely rejects
             hundreds of NPCs, so expanding everything would bury the reasons the user actually
             has to act on. -->
        <TreeView Grid.Row="1"
                  ItemsSource="{Binding Issues}"
                  Background="{DynamicResource TextBoxBackground}"
                  Foreground="{DynamicResource PrimaryForeground}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Standard"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <TreeView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </TreeView.ItemsPanel>
            <!-- Declared inline rather than as DataType-keyed resources (as in the rejected-NPCs
                 tree in SettingsView): the tree is exactly three fixed levels, so naming the
                 templates outright means each row's expand chevron never depends on implicit
                 template lookup succeeding for a recycled container. -->
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Mods}">
                    <TextBlock Text="{Binding DisplayText}" ToolTip="{Binding Reason}"/>
                    <HierarchicalDataTemplate.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Npcs}">
                            <TextBlock Text="{Binding DisplayText}" ToolTip="{Binding ModName}"/>
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DisplayText}" ToolTip="{Binding ToolTipText}"/>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.Resources>
                <!-- Retemplated here rather than per theme, like the rejected-NPCs tree in
                     SettingsView: the default TreeViewItem paints its selection with SystemColors,
                     which reads as a light-mode control in the dark themes. Implicit (unkeyed) so
                     it reaches items at all three levels. -->
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="Padding" Value="2,2"/>
                    <!-- Rows carry FormKeys, so they are routinely wider than the window and
                         horizontal scrolling stays on. Selecting one would then scroll the tree
                         right by the width of the chevron column and put the expanders out of
                         reach; the handler keeps the vertical half and drops the horizontal. -->
                    <EventSetter Event="RequestBringIntoView" Handler="TreeViewItem_RequestBringIntoView"/>
                    <Setter Property="ItemsPanel">
                        <Setter.Value>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel/>
                            </ItemsPanelTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TreeViewItem">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="16"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition/>
                                    </Grid.RowDefinitions>

                                    <ToggleButton x:Name="Expander"
                                                  Grid.Row="0" Grid.Column="0"
                                                  IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press" Focusable="False">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent" Width="16" Height="16">
                                                    <Path x:Name="Chevron"
                                                          Data="F1 M 0,0 L 5,0 L 2.5,3 Z"
                                                          Stretch="Uniform" Width="7" Height="7"
                                                          Fill="{DynamicResource PrimaryForeground}"
                                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                                          RenderTransformOrigin="0.5,0.5"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="False">
                                                        <Setter TargetName="Chevron" Property="RenderTransform">
                                                            <Setter.Value>
                                                                <RotateTransform Angle="-90"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <Border x:Name="Bd"
                                            Grid.Row="0" Grid.Column="1"
                                            Background="Transparent"
                                            CornerRadius="2"
                                            Padding="{TemplateBinding Padding}"
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter x:Name="PART_Header"
                                                          ContentSource="Header"
                                                          HorizontalAlignment="Left"
                                                          VerticalAlignment="Center"/>
                                    </Border>

                                    <ItemsPresenter x:Name="ItemsHost"
                                                    Grid.Row="1" Grid.Column="1"/>
                                </Grid>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsExpanded" Value="False">
                                        <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed"/>
                                    </Trigger>
                                    <Trigger Property="HasItems" Value="False">
                                        <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background"
                                                Value="{DynamicResource SelectedItemBackground}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TreeView.Resources>
        </TreeView>

        <TextBlock Grid.Row="2" Margin="0,10,0,0" TextWrapping="Wrap"
                   Text="{l:Loc skipThemAndContinue}"/>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="{l:Loc copyText}" Command="{Binding CopyTextCommand}"
                        Height="30" Padding="12,0"
                        ToolTip="{l:Loc copyTheWholeList}"/>
                <Button Content="{l:Loc copySheet}" Command="{Binding CopySheetCommand}"
                        Height="30" Padding="12,0" Margin="10,0,0,0"
                        ToolTip="{l:Loc copyOneTabSeparated}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{l:Loc skipAndContinue}" Click="ContinueButton_Click"
                        Height="30" Padding="12,0" IsDefault="True"
                        ToolTip="{l:Loc patchTheValidSelections}"/>
                <Button Content="{l:Loc cancelRun}" Click="CancelButton_Click"
                        Height="30" Padding="12,0" Margin="10,0,0,0" IsCancel="True"
                        ToolTip="{l:Loc abortWithoutPatchingAnything}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>

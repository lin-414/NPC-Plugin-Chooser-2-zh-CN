<Window x:Class="NPC_Plugin_Chooser_2.Views.RandomizeResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:l="clr-namespace:NPC_Plugin_Chooser_2.Localization" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
        xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:VM_RandomizeResultsWindow}"
        Title="{l:Loc randomizeCompleteWithWarnings}"
        Height="620" Width="1000"
        MinHeight="360" MinWidth="620"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="True"
        Style="{DynamicResource ThemedWindowStyle}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>

    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding SummaryText}"
                   FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,8"/>

        <!-- Run-level notes (inherited-template faces, removed selections, the not-in-load-order
             explanation) — outcomes and context, kept out of the per-NPC table. -->
        <Border Grid.Row="1" Margin="0,0,0,8" Padding="8" CornerRadius="3"
                Background="#22808080" BorderBrush="#55808080" BorderThickness="1"
                Visibility="{Binding HasNotes, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="{Binding NotesText}" TextWrapping="Wrap" FontStyle="Italic"/>
        </Border>

        <!-- Filter -->
        <Grid Grid.Row="2" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{l:Loc filter}" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox Grid.Column="1"
                     Text="{Binding FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center"
                     ToolTip="{l:Loc filterRowsByAny}"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox Content="{l:Loc showTemplatedNpcs}"
                          IsChecked="{Binding ShowTemplatedNpcs, Mode=TwoWay}"
                          VerticalAlignment="Center" Margin="12,0,0,0"
                          Visibility="{Binding HasTemplatedRows, Converter={StaticResource BooleanToVisibilityConverter}}"
                          ToolTip="{l:Loc templatedNpcsCopyTheir}"/>
                <CheckBox Content="{l:Loc showUnloadedNpcs}"
                          IsChecked="{Binding ShowUnloadedNpcs, Mode=TwoWay}"
                          VerticalAlignment="Center" Margin="12,0,0,0"
                          Visibility="{Binding HasUnloadedRows, Converter={StaticResource BooleanToVisibilityConverter}}"
                          ToolTip="{l:Loc unloadedNpcsBaseNpc}"/>
            </StackPanel>
        </Grid>

        <!-- One row per affected NPC, sortable by any column. Cell selection + ExcludeHeader:
             highlight any run of cells and Ctrl+C pastes just those cells, tab-separated; the
             Copy Sheet button below emits the full headered table. Styled inline like
             ValidationResultsWindow — the theme files carry NO implicit DataGrid styles.
             ShowResults appends one fixed-width "Candidate N" column per tried-and-rejected
             candidate (exhausted-pool rows); with enough of them the grid scrolls horizontally,
             and the MinWidths below are what the star columns clamp to instead of collapsing. -->
        <DataGrid Grid.Row="3"
                  x:Name="ResultsGrid"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ItemsSource="{Binding FilteredRows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Extended"
                  SelectionUnit="CellOrRowHeader"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  ClipboardCopyMode="ExcludeHeader"
                  VirtualizingPanel.ScrollUnit="Pixel"
                  Background="{DynamicResource SecondaryBackground}"
                  Foreground="{DynamicResource PrimaryForeground}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  RowBackground="{DynamicResource SecondaryBackground}"
                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                  VerticalGridLinesBrush="{DynamicResource BorderBrush}">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{l:Loc copySelectedCells}"
                              Command="ApplicationCommands.Copy"
                              InputGestureText="{l:Loc ctrlC}"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="6,4"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Padding" Value="4,2"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                            <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.CellStyle>
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="{l:Loc issue}" Binding="{Binding Issue}" Width="1.5*" MinWidth="170">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="{l:Loc nPC}" Binding="{Binding Npc}" Width="1.2*" MinWidth="140"/>
                <DataGridTextColumn Header="{l:Loc formKey}" Binding="{Binding FormKey}" Width="160"/>
                <DataGridTextColumn Header="{l:Loc detail}" Binding="{Binding Detail}" Width="2*" MinWidth="180">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="4" Margin="0,12,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="{l:Loc copySheet}" Command="{Binding CopySheetCommand}"
                        Height="30" Padding="12,0"
                        ToolTip="{l:Loc copyOneTabSeparated}"/>
            </StackPanel>
            <Button Content="{l:Loc close}" Click="CloseButton_Click" HorizontalAlignment="Right"
                    Height="30" Width="100" IsDefault="True" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>

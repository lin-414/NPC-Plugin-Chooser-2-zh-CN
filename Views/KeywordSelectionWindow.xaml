<Window x:Class="NPC_Plugin_Chooser_2.Views.KeywordSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:l="clr-namespace:NPC_Plugin_Chooser_2.Localization"
        mc:Ignorable="d"
        Title="{l:Loc setKeywords}"
        Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Style="{StaticResource ThemedWindowStyle}">

    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{l:Loc settingKeywordsFor}" 
                   FontWeight="SemiBold"
                   Margin="0,0,0,5"/>
        
        <TextBlock Grid.Row="1" 
                   x:Name="ModNameTextBlock"
                   Text="{l:Loc modName}"
                   Foreground="{DynamicResource PrimaryForeground}"
                   FontWeight="Bold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,15"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel: Current Mod's Keywords -->
            <GroupBox Grid.Column="0" Header="{l:Loc currentKeywords}" Margin="0,0,5,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,5,5,5">
                        <Button Content="{l:Loc removeSelected}" 
                                x:Name="RemoveSelectedButton"
                                Click="RemoveSelectedButton_Click"
                                Padding="10,3" 
                                ToolTip="{l:Loc removeTheSelectedKeyword}"/>
                        <Button Content="{l:Loc clearAll}" 
                                x:Name="ClearAllButton"
                                Click="ClearAllButton_Click"
                                Padding="10,3"
                                Margin="5,0,0,0"
                                ToolTip="{l:Loc removeAllKeywordsFrom}"/>
                    </StackPanel>
                    
                    <ListBox Grid.Row="1" 
                             x:Name="CurrentKeywordsListBox"
                             SelectionMode="Single"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5,3"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Grid Grid.Row="2" Margin="5,5,5,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" 
                                 x:Name="NewKeywordTextBox"
                                 VerticalContentAlignment="Center"
                                 ToolTip="{l:Loc typeANewKeyword}"/>
                        <Button Grid.Column="1" 
                                Content="{l:Loc add}" 
                                x:Name="AddNewKeywordButton"
                                Click="AddNewKeywordButton_Click"
                                Padding="10,3" 
                                Margin="5,0,0,0"
                                ToolTip="{l:Loc addTheTypedKeyword}"/>
                    </Grid>
                </Grid>
            </GroupBox>

            <!-- Center: Transfer Buttons -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5">
                <Button Content="{l:Loc ltLtAdd}" 
                        x:Name="AddFromOthersButton"
                        Click="AddFromOthersButton_Click"
                        Padding="10,5" 
                        Margin="0,5"
                        ToolTip="{l:Loc addTheSelectedKeyword}"/>
            </StackPanel>

            <!-- Right Panel: Keywords from Other Mods -->
            <GroupBox Grid.Column="2" Header="{l:Loc keywordsFromOtherMods}" Margin="5,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5,5,5,5">
                        <TextBlock Text="{l:Loc filter}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="FilterTextBox" 
                                 MinWidth="150" 
                                 TextChanged="FilterTextBox_TextChanged"
                                 ToolTip="{l:Loc filterTheListOf}"/>
                    </StackPanel>
                    
                    <ListBox Grid.Row="1" 
                             x:Name="OtherKeywordsListBox"
                             SelectionMode="Single"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             MouseDoubleClick="OtherKeywordsListBox_MouseDoubleClick">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" Margin="5,3"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </GroupBox>
        </Grid>

        <TextBlock Grid.Row="3" 
                   x:Name="StatusTextBlock"
                   Text="{l:Loc doubleClickAKeyword}"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource DisabledForeground}"
                   Margin="0,10,0,10"/>

        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="{l:Loc oK}" 
                    x:Name="OkButton"
                    Click="OkButton_Click"
                    IsDefault="True"
                    MinWidth="80" 
                    Padding="15,5" 
                    Margin="0,0,10,0"/>
            <Button Content="{l:Loc cancel}" 
                    x:Name="CancelButton"
                    Click="CancelButton_Click"
                    IsCancel="True"
                    MinWidth="80" 
                    Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>

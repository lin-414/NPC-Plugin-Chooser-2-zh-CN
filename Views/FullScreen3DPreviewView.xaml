<reactiveui:ReactiveWindow x:Class="NPC_Plugin_Chooser_2.Views.FullScreen3DPreviewView"
                           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                           xmlns:local="clr-namespace:NPC_Plugin_Chooser_2.Views"
                           xmlns:vm="clr-namespace:NPC_Plugin_Chooser_2.View_Models"
                           xmlns:themes="clr-namespace:NPC_Plugin_Chooser_2.Themes"
                           xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                           xmlns:reactiveui="http://reactiveui.net"
                           mc:Ignorable="d"
                           x:TypeArguments="vm:VM_FullScreen3DPreview"
                           Title="{Binding Title, FallbackValue='3D Preview'}"
                           Width="1000" Height="800"
                           MinWidth="640" MinHeight="480"
                           WindowStartupLocation="CenterOwner"
                           ShowInTaskbar="True"
                           Style="{DynamicResource ThemedWindowStyle}"
                           d:DataContext="{d:DesignInstance Type=vm:VM_FullScreen3DPreview}">
    <i:Interaction.Behaviors>
        <themes:TitleBarThemingBehavior />
    </i:Interaction.Behaviors>

    <Grid>
        <!-- ShowSettingsControls=False hides the Load/Reload/Reset row that's
             only useful in the Settings panel. IsFullBodyOrbitMode=True swaps
             the camera into "show whole NPC + free orbit" mode and skips
             persistence to settings. -->
        <local:UC_InternalMugshotPreview x:Name="PreviewControl"
                                          DataContext="{Binding Inner}"
                                          ShowSettingsControls="False"
                                          IsFullBodyOrbitMode="True"/>
    </Grid>
</reactiveui:ReactiveWindow>
